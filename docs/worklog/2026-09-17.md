# 작업 내역 2026-09-17 (수)

커밋: `1cf5c9e`(체결쏴 주식 코어 연결 외), `59361bc`(시간 초과 30초·메인 창 연동) — `main` 푸시 완료.
빌드: `dist/meme_0917_2.zip` (17:14). 검증 루프 757개 통과.

## 1. 체결쏴 주식 — 코어 연결 완료
- 코어에 종목별 주식 종목 상태(`종목|stock`)·판정·주문 라우팅을 붙여 모의 계좌로 여러 판 실행(선주문 1주 → HL 1계약, 체결차 0).
- 거래소 콤보(KRX/NXT) 추가. 기본 NXT, 실행 중에도 잠기지 않고 고르면 바로 코어에 반영.
  LS 현물주문 거래소 필드는 공식 문서로 확인(`MbrNo`: KRX/NXT). **모의투자는 NXT 요청을 KRX로 접수** → 운영에서 확인.
- 신용 세트: 진입 003(유통/자기융자신규)·청산 101(유통융자상환), 추측값. **모의투자는 신용 주문 거부(01409 현금거래만)** → 운영에서 확인.
  기본 신용 2세트(아래 2개)의 기본값을 코어 종목 상태로 이관(화면만 갖고 있어 코어 연결 때 풀렸음).
- NXT 전용 시세(NH1/NS3) 구독, 종목 VI(VI_) 발동/해제 구독 → 발동 중 주식 선주문 중단, 해제 뒤 재개 딜레이. VI 구분값은 첫 발동 로그로 확인 필요.
- 주식 Sprd(누적·마지막 판)가 비던 것 수정(SF이론가 없어도 S현재가로 기준값 기록).
- 주식 화면이 주식선물 공통설정·리스크값을 읽어 "시간 2구간 저장 안 됨"으로 보이던 것 수정.

## 2. 자동M 공통 규칙 변경(사용자 결정)
- 선주문 연속 3회 거부 → 실행 끔에서 **세트 중지(검정)**로(결정 29 정정).
- 재시동 때 중지 세트는 **대기(정상 표시)로 복원**, 장부는 유지(결정 22 정정).
- 실행 중 **종목 콤보 잠금**(09-04 규칙 확인, 결정 15의 "항상 열림" 폐기). 이 건은 두 번 헛돌아 되돌린 뒤 확정 — 사용자 말과 기록이 어긋나면 기록으로 뒤집지 말고 먼저 확인(메모리 기록).
- 주문 TR(발주·정정·취소) REST 시간 초과 10초 → **30초**(결정 39 정정). 조회는 10초 그대로.

## 3. 화면 결함 수정
- 세트 입력 동기화가 전 세트를 덮어 미전송 인라인 괴리율이 옛 값으로 되돌아가던 것 → 바뀐 세트만 다시 채움.
- 기준수량 고치고 '적'을 누르면 옛 값으로 되돌아가던 것(버튼 포커스 이동 순서) 수정.
- 체결차 칸 정수 반올림(0.612 → "1") → 소수 그대로.
- 상태줄은 가장 최근에 바뀐 메시지가 앞에.
- 창 저장이 슬롯 번호를 따라가 재실행 때 값이 사라지던 것 → 새 창은 같은 창의 최근 저장값을 물려받음.
- 주문리스트에 LS 취소·거부가 안 보이던 것(취소내역 기록이 HL 통보에만 연결) 수정, 상태 취소/거부 구분.

## 4. 메인 창
- 메인 최소화 → 모든 화면 최소화, 메인 복원 → 모든 화면 복원(별도 프로세스라 각 화면이 메인 창 상태를 따라감).
- 작업표시줄 단추를 하나로(앱 식별자 공통). 알려진 한계: 묶음 단추에서 화면 하나만 올리면 메인은 안 올라옴(사용자 "이 상태로").

## 5. 운영 로그 조사(real_log/ws_ls_20260917)
- 타임아웃 2건(10:41 역방향 2세트 진입, 14:11 역방향 3세트 청산): LS 주문 서버 지연이 원인. 접수 통보를 주문번호로 대조 → 두 주문 모두 LS 미접수(유령·이중 주문 없음). 취소 응답 지연 3건은 10~13초 뒤 정상 처리, 12:07 LS 503 응답 1건은 미접수 처리. WS 끊김은 아님(재연결 기록 없음, 시세 갱신 지속).
- 새로고침 초당 한도 경고 2건: 우리 가드가 두 번째 새로고침을 막은 것, 주문 무관.
- 자동 행방 확인(타임아웃 뒤 미체결·포지션 조회로 자동 판단) 안은 검토 뒤 보류.

## 6. 남은 것 / 운영 확인 대기
- 운영 계좌에서 확인: NXT 주문 접수 여부, 신용 코드(003/101) 수락 여부, VI 구분값.
- 주식 체결차 중지 기준(1회주문수량 이상 vs 0이 아니면 중지) — 결정 대기.
- 새로고침 겹침 합치기(경고 제거) — 원하면.
