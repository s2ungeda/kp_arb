# DESIGN-auto-m.md — 자동M 전략 (체결쏴, HL–주식선물 maker→taker)

> **상태:** 작성 중(초안). `DESIGN.md §6.2`의 자동M 부분을 떼어 상세화한다. **계약의 일부.**
> **원본:** 화면 목업 `docs/STG_2/`(`layout_1.png`·`체결쏴 설정.png`·`세트설정.png`, 2026-08-26) + 사용자 문답(2026-08-26, `체결쏴(자동M).txt`).
> **화면 코드:** 아직 없음(자동T = `kp_arb/order_autot.py` 참고). **발주 로직 미구현.**

## 1. 개요
- 자동M = **HL perp ↔ 국내 주식선물(SF)** 한 쌍. **선주문(LS SF maker) 체결 → 후주문(HL taker)**. 동시 taker인 자동T(HL–주식)와 별개 화면.
- 주로 **장중** 사용.
- 한 종목에 **정방향·역방향 동시 진행** 가능(정방향=SF 롱 / 역방향=SF 숏). **SF는 선물이라 숏에 업틱룰이 없다**(자동T 대주와 다른 점).
- **SF 1계약 = 주식 10주 = HL 10계약.** 1회주문 단위 = **계약**.

## 2. 방향 정의
| 방향 | 시장 상태 | 진입 액션 | 청산 액션 |
|---|---|---|---|
| **정방향** | HL 고평가 / LS 저평가 | `-HP/+SF` : HL 매도 + SF 매수 | `+HP/-SF` : HL 매수 + SF 매도 |
| **역방향** | LS 고평가 / HL 저평가 | `+HP/-SF` : HL 매수 + SF 매도 | `-HP/+SF` : HL 매도 + SF 매수 |

- SF 매도 = 선물 숏(공매도 아님). `DESIGN §3` 공매도 불변식과 무관.

## 3. 판정
**진입은 SF·S(현물) 괴리 둘 다(AND), 청산은 SF 괴리만.** 각 세트는 계약수로 est를 구해 실시간 괴리를 계산한다.

| 신호 | 발주 조건 | 액션 |
|---|---|---|
| 정방향 진입 | (실시간 SF괴리 > `-HP/+SF`) **AND** (실시간 S괴리 > `-HP/+S`) | HL 매도 + SF 매수 |
| 정방향 청산 | 실시간 SF괴리 < `+HP/-SF` | HL 매수 + SF 매도 |
| 역방향 진입 | (실시간 SF괴리 < `+HP/-SF`) **AND** (실시간 S괴리 < `+HP/-S`) | HL 매수 + SF 매도 |
| 역방향 청산 | 실시간 SF괴리 > `-HP/+SF` | HL 매도 + SF 매수 |

- **SF괴리** = HL_est괴리 − SF괴리(스프레드), **S괴리** = HL_est괴리 − 현물괴리. 부등호 방향은 자동T와 동일(정방향 진입 `>`, 청산 `<` / 역방향 반대).
- **S(현물) 괴리는 진입 허용 조건으로만** 쓰고, 실제 매매는 SF+HL. 청산은 S를 안 본다. S 괴리의 est 수량 = **계약수 × 10 주** 등가(확정 2026-08-26).
- 편집 가능 컬럼은 방향당 3개: 진입 `SF`·진입 `S`·청산 `SF`.

## 4. 실행 (선주문 maker → 후주문 taker)
- **주문순서:** 선주문(LS SF maker) 체결 → 후주문(HL taker) 발주. **후주문 수량 = 선주문 체결수량 × 10.**
- **선주문 정정 트리거(확정 2026-08-26):** 시세 수신 때마다 실시간 괴리율·역산가를 다시 계산하고, **역산가가 바뀌면 자동으로 취소 → 재발주**한다.
- **선주문 딜레이(공통설정, 목업 1초):**
  - 후주문 **전량 체결** 후, 딜레이 뒤 (다음) 선주문 발주.
  - **선주문 부분체결** 상태에서 가격정정 상황 발생 시: **후주문 취소 → 딜레이 뒤 후주문 재발주**(오타 정정 2026-08-26 — "후주문 부분체결" 아님).
  - **선주문 정정:** 선주문 취소 → 딜레이 뒤 정정가로 선주문 재발주.
- **전환딜레이(초, 세트별):** 진입 마지막 체결 후 N초 뒤 청산 주문 가능, 청산 마지막 체결 후 N초 뒤 진입 주문 가능.
- **실패 처리(`DESIGN §6.2-5` 유지):** 선주문(LS) 거부 → 자동중단 + 사운드 알람. 후주문(HL) 거부 → 재시도, 재시도도 거부 시 자동중단 + 알람(선주문 이미 체결이라 헤지 필수).

## 5. 주문 계약수
- **진입** = `Min(1회주문수량, 목표수량 − RT선진입)`
- **청산** = `Min(1회주문수량, RT선진입)`
- **후주문(HL) 수량** = 선주문 체결수량 × 10

## 6. 선주문 가격 (역산 → 호가단위 반올림 → 진입범위)
### 6.1 역산 (`disparity.maker_price_for_spread`)
스프레드 = HL_est괴리 − 국내괴리이고, 선주문은 우리가 가격 P를 정하는 maker라 국내괴리에 P가 들어간다. 스프레드 = 기준값이 되게 P를 풀면:

```
P = 기준가_SF × (1 + HL_est괴리 − 기준값)
```

- **기준가_SF** = 선물이론가.
- **HL_est괴리** = 후주문 수량(=계약수×10)만큼 HL 호가창을 쓸어담은 est 괴리(환율 fx 반영, 자동T와 동일).

| 경우 | SF | HL | HL_est 호가창 | 기준값 |
|---|---|---|---|---|
| 정방향 진입 | 매수 | 매도 | 매수호가창(bid) | `-HP/+SF` |
| 정방향 청산 | 매도 | 매수 | 매도호가창(ask) | `+HP/-SF` |
| 역방향 진입 | 매도 | 매수 | 매도호가창(ask) | `+HP/-SF` |
| 역방향 청산 | 매수 | 매도 | 매수호가창(bid) | `-HP/+SF` |

### 6.2 호가단위 반올림
- **종목별 선주문 호가단위**(공통설정: 하이닉스 3,000 / 삼성전자 500 / 현대차 1,000)로:
  - SF **매수 → 내림**, SF **매도 → 올림** (기준값보다 불리해지지 않게).

### 6.3 진입범위 (발주 여부)
- SF **매수**(정방향 진입·역방향 청산): 한계 = (상대**매도**호가 − 1틱) × (1 − 범위) = **가장 낮은 허용가**. 발주조건 **P ≥ 한계**.
- SF **매도**(정방향 청산·역방향 진입): 한계 = (상대**매수**호가 + 1틱) × (1 + 범위) = **가장 높은 허용가**. 발주조건 **P ≤ 한계**.
- **범위** = 공통설정(목업 0.4%). **상대호가** = 공통설정 콤보(상대 1~5호가).
- 한계 = "호가에서 이만큼까지만 물러나 걸 수 있다"는 가장 수동적인 가격. 역산가가 그보다 더 물러나 있으면(체결 가망 낮음) **미발주 / 이미 나가 있으면 취소**.

## 7. RT선진입 · 목표수량 (자동T와 동일)
- 진입 체결 → **RT선진입 증가**, 청산 체결 → **감소**.
- **진입 가능:** RT선진입 + 계약수 ≤ **목표수량**.
- **청산 가능:** RT선진입 ≥ 계약수.
- **목표수량 도달 후에도 계속 계산·주문 시도**(청산으로 RT선진입이 줄어 진입 여지가 다시 생김).
- "RT 진입수량 수동 입력"으로 덮어쓰기 가능.

## 8. 체결차 (자동T와 같은 개념 — 확정 2026-08-26)
- **체결차 = SF(LS) 잔고 × 10 + HL 잔고**(매도 −). **SF 1계약 = HL 10계약이므로 SF 잔고에 ×10.** 완전 헤지면 0(예: SF 매수 n계약 → +10n, HL 매도 10n → −10n).
- ≠0 → 시스템 정지 + 알람, 사용자 수동 처리. 세트설정 "체결차 Clear"로 수동 0 초기화.

## 9. 화면 구성 (목업 STG_2/layout_1.png)
- **상단:** 종목 콤보 · HL 호가단위 · `적` · **기준수량** · 정방향[모니터 3칸] · 역방향[3칸] · `설정`.
  - 모니터 3칸 = **[진입 SF · 진입 S · 청산 SF]**(입력 순서와 동일 — 확정 2026-08-26. 목업 정방향 -0.82/-0.82/-0.52, 역방향 0.12/0.12/0.23).
- **정방향 / 역방향** 각 **진입 / 청산 3세트**, 세트별 컬럼:
  목표수량 · 1회주문수량 · **진입 `-HP/+SF`** · **진입 `-HP/+S`** · 진입 실행 버튼 · **청산 `+HP/-SF`** · 청산 실행 버튼 · `설정` · RT선진입 · 체결차 · **초**(전환딜레이) · Sprd(진입 누적) · Sprd(청산 누적)
  (역방향은 `+HP/-SF`·`+HP/-S`·`-HP/+SF`)
- **인라인 수정:** 진입 `SF`·진입 `S`·청산 `SF` 세 칸만. **진입 실행 중 → 진입 SF·S 잠금 / 청산 실행 중 → 청산 SF 잠금.**
- **매매결과(진입/청산):** 그리드 위 `진입`/`청산` 버튼이 **누적체결량 표시 + clear 겸용**. 누적 계산은 자동T `DESIGN-auto-t.md §7a`와 동일(확정 2026-08-26). 아래 §9a에 SF 기준으로 옮겨 적음(사용자 확인용).
- **세트설정 창:** 목표수량 · 1회주문수량 · **전환딜레이(초)** · 진입SF · 진입S · 청산 · ☐RT 진입수량 수동 입력 · ☐체결차 Clear
- **공통설정 창(체결쏴 설정):** 주문가능시간 2구간 · 선주문 호가단위(종목별) · 선주문 딜레이(초) · 선주문 범위(%) · 매수/매도 상대호가 콤보 · 정/역 리스크방지

## 9a. 매매결과 누적 계산 (체결마다 갱신 — 자동T §7a를 SF로, **사용자 확인 요청**)
매매결과 라벨은 목업 그대로 `-HP`/`+S`/`-환`이나, 자동M의 국내 체결은 **SF**다. **정방향 진입** 예:

- **`-HP`** = 누적 **HL 매도체결 수량**
- **`+S`** = 누적 **SF 매수체결 수량**(계약)
- **`-환`** = **∑(원달러선물 상대호가 × HL 체결수량) ÷ ∑(HL 체결수량)** — 체결수량 가중평균 환율(원달러선물 호가 없으면 직전종가)
- **`Sprd`** = 누적(실현) 스프레드 = **(누적 HL 매도 평균체결가 괴리) − (누적 SF 매수 평균체결가 괴리)**, HL 괴리 환산에 위 `-환` 사용
- **역방향**은 부호 반대(`+HP`/`-S`/`+환`).
- `clear` 버튼 = 위 누적값 0으로 초기화(세트설정 "체결차 Clear"는 체결차만 — 별개).

> **[확인 요청]** 위 `Sprd` 실현식(HL 평균괴리 − SF 평균괴리)이 의도와 맞는지 검토 바람.
- **정방향 진입:** `-HP/+SF`·`-HP/+S` ≤ `[0]` 입력 금지 + 경고.
- **정방향 청산:** `+HP/-SF` ≥ `[0.5]` 입력 금지 + 경고.
- **정방향:** `-HP/+SF` > (`+HP/-SF` + `[0.1%]`) 아니면 입력 금지 + 경고.
- **역방향 진입:** `+HP/-SF`·`+HP/-S` ≥ `[0]` 입력 금지 + 경고.
- **역방향 청산:** `-HP/+SF` ≤ `[0.5]` 입력 금지 + 경고.
- **역방향:** (`+HP/-SF` + `[0.1%]`) < `-HP/+SF` 아니면 입력 금지 + 경고.

## 11. 주문가능시간 (2구간, 초단위)
- **사용자가 직접 입력**(확정 2026-08-26) — 공통설정창에서 **2구간, 초 단위**로. 고정 기본값 없음. 목업값 `08:30:10~08:46:20 / 15:35:30~15:46:55`는 화면 예시일 뿐.
- `DESIGN.md §6.2`의 `8:45~15:35`는 옛 표기 — 사용자 입력값이 우선.

## 12. 용어
- **기준수량 · RT선진입 · 체결차 · 전환딜레이 · HP(%)** — 자동T `DESIGN-auto-t.md §8`과 동일.
- **선주문/후주문** — 선주문 = LS SF maker, 후주문 = HL taker(선주문 체결 후).
- **선주문 딜레이 · 선주문 범위 · 선주문 호가단위 · 상대호가 콤보** — §4·§6 정의.

## [OPEN]
- §9a `Sprd` 실현식(HL 평균괴리 − SF 평균괴리)이 의도와 맞는지 사용자 검토 대기(나머지 1~7 문답은 2026-08-26 확정).
- 세부 발주 상태기계(선주문↔후주문 전이·부분체결·정정 순서)는 구현 단계에서 상세화.
