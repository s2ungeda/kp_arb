# DESIGN — 수동 주문 화면 (일반 주문창)

> [DESIGN.md](DESIGN.md) §6.3의 상세. 계약의 일부다(DESIGN.md와 함께 읽는다).
> 원본 참조: 델파이 `C:\project\Dalin_2026\Src\wins\UNormalOrderEx.pas/.dfm`.
> **개정 2026-08-06** — 원본 문서 `밈전략0805.xlsx` §2.1 요구사항 반영.
> 새로 들어온 것은 **[신규]** 로 표시했다. 표시 없는 문단은 기존 확정분 그대로다.
> **미결 없음** — §5 확인 이력 참조.

## 6.3 수동 주문 화면 (일반 주문창, v1=MVP — 확정 2026-08-03)

사람이 직접 한 건씩 주문을 내는 창. 자동 전략(§6.2)과 **독립**. 화면은 코어에 명령만 보내고 주문·검증·기록은 코어(§6.2-0 구조 동일: 코어 클라이언트, 화면 스레드 네트워크 금지).

**HL/LS는 UI가 크게 달라 화면을 분리한다**(확정 2026-08-05) — **HL 전용 `order_hl`**(먼저 구현, main.bat 메뉴 "HL 일반주문"), **LS 전용 `order_ls`**(후속). **코어 명령(`manual_*`)·스냅샷(`/manual_state`)은 두 화면 공용.** 아래 §1은 HL 창 기준(LS 창은 공매도·계좌 표시 등이 달라 별도 확정).

**경계 재설정**: 델파이 원본은 화면이 직접 엔진(TradeCore/ApiManager/QuoteBroker/TradeBroker)에 붙어 주문했다. 우리는 그 능력을 **코어**가 갖고(이미 있음: `place_order`/`cancel_order`, `get_open_orders`/`get_positions`/`get_balance`, 호가 사다리 `quote.bids/asks`), 화면은 입력·표시만 한다.

### 1) HL 창(`order_hl`) 구조 (확정 2026-08-05, 개정 2026-08-06)

**화면안: [order_hl_layout.png](docs/order_hl_layout.png)** — 아래 글과 그림이 다르면 **글이 우선**이다.

> 그림에서 주문 버튼 캡션이 `이닉스매수 주` 로 잘려 보이는 것은 **버튼 폭이 좁아서**다.
> 실제로는 2줄 전체(`SK하이닉스` / `매수 주문`)가 다 보이도록 폭을 잡는다.
> 그림은 배치를 보는 용도이며, 각 항목의 정확한 동작은 아래 글로 정한다.

델파이 원본(`UNormalOrderEx`) 레이아웃 — **좌(입력+잔고) / 우(호가창) 2분할**. HL perp 전용이라 거래소 콤보 없음.
**[신규] 창 크기는 델파이 원본보다 120% 로 키운다.**

- **좌 상단**: 종목 콤보(취급 3종목의 HL perp) + **'적' 버튼**(같은 줄).
  '적'은 **누를 때만** 그 종목을 활성화하고 잔고/포지션을 재조회한다
  (`manual_refresh`→`refresh_snapshot`, OrderBook 재동기). **콤보만 바꿔선 안 바뀐다**(델파이 SetSymbol).
  콤보 휠만 굴려도 종목이 바뀌면 주문 직전에 대상이 슬쩍 바뀔 수 있어 2단계로 둔다.
  같은 종목에서 다시 누르면 **잔고 재동기**가 되므로 별도 새로고침 버튼은 두지 않는다(§1-6).
  → 콤보에 고른 종목과 활성 종목이 다를 때는 **콤보 배경색을 달리하거나 '적'을 강조**해 알려준다.
  호가단위 머지 콤보는 **오더북 위**(우측, `manual_hl_merge`→`set_hl_aggregation`).
- **[신규] 좌 상단 — 레버리지 버튼**: 종목 콤보 바로 아래. **버튼 캡션에 지금 걸린 값을 보여준다**(예: `Cross  10 x`).
  누르면 설정 팝업이 뜬다. 자세한 동작은 §1-3.
- **[신규] 좌 상단 — 마진모드 라벨**: 레버리지 버튼 옆(화면안의 `Unified`). **표시 전용 라벨**이다.
  포트폴리오 마진이 켜져 있는지 보여주기만 하고, 여기서 바꾸지 않는다(§1-3).
- **좌 중단**: 수량, 단가. **지정가만**(시장가 제외). **호가/가격 모드 + 틱 오프셋** — **호가 모드**: 단가 = (매수=매수1호가 / 매도=매도1호가) + N틱, 시세 따라 자동 갱신. **가격 모드**: 호가 클릭 시 그 가격 그대로(틱 미적용). **가격은 틱에서 뽑은 소수 자리수로 통일**. **Reduce/Post 체크박스**(reduce_only / post_only=Alo 메이커).
- **[신규] 좌 중단 — 주문 방식**: **교차/격리**와 **레버리지(기본 5배)** 를 화면에서 고른다. Reduce only 는 기존 체크박스 그대로. **주문과 별개의 설정이다 — §1-3 참조.** (레버리지는 v2 [OPEN]이었으나 v1으로 올린다.)
- **좌 버튼** *(확정 2026-08-06 — 사용자 화면안)*: **매수/매도 라디오 + 큰 색버튼 하나.**
  라디오로 방향을 고르고, 버튼이 그에 맞춰 바뀐다.
  - 캡션은 **2줄** — 첫 줄 종목명, 둘째 줄 `매수 주문` / `매도 주문`.
  - 배경색은 **매수 빨강 / 매도 파랑**, 글자는 **흰색 굵게**.
  - **버튼 폭은 가장 긴 종목명(`SK하이닉스`) 기준으로 잡는다** — 잘리면 어느 종목인지 못 읽는다.
- **[신규] 원클릭 체크박스 — 안전 잠금** *(개정 2026-08-07 — 확인창 방식 폐기)*: **체크돼 있을 때만** 주문 버튼
  클릭 시 발주된다. **체크 안 돼 있으면 발주하지 않고** 하단 상태바에 "원클릭 미체크 — 주문 안 나감"을 표시한다.
  확인창(팝업)은 없다. **기본값은 켜짐.**
- **하단 잔고표** *(개정 2026-08-06)*: 창 아래쪽에 **2열 그리드**로 편다.
  왼쪽 = `수량 · 진입금액 · 진입가 · PNL · Liq_Prc`, 오른쪽 = `Margin · Funding · Oracle · FundRate · CountDown`.
펀딩은 셋으로 나뉘어 있다 — `Funding`(누적 수익) / `FundRate`(펀딩률) / `CountDown`(남은 시간). §1-1 참조.
  값은 **실시간으로 갱신된다** — §1-5 참조. (HL은 공매도 제약이 없어 매도가능 항목이 없다. LS 창에서만.)
  - **실시간 갱신 두 층(확정 2026-08-10)**: ① **즉시 계산 가능**(잔고=포지션·평단·PNL·진입금액·Oracle·FundRate)은 **체결 이벤트/시세로 바로** 갱신(조회 없음). ② **거래소만 계산하는 값**(`Margin`·`Liq_Prc`·`Funding`=누적펀딩)은 클라이언트가 못 만들어, **체결 발생 시 clearinghouse 1회 재조회**로 갱신한다. 재조회는 **디바운스 코얼레싱**(`_HL_FILL_DEBOUNCE_S`=0.5s) — 체결이 몰려도 창당 1회로 합침. **외부(홈페이지) 체결·포지션도** userFills로 잡아 `OrderBook.replace_positions`로 HL 포지션만 실측 교체(적 불필요). ②에 해당하는 항목은 **라벨을 회색**으로 구분한다.
- **우 호가창**: 헤더 없이 `가격 | 잔량` 2열(색으로 구분), 매도호가 파랑(위)·매수호가 빨강(아래). **현재가와 같은 호가만 노랑 바탕**(별도 현재가 행 없음). 가격 모드에서 클릭 시 그 가격을 단가에. **[신규] 5호가까지 보여주고 0.5초마다 갱신한다** (§1-4 — 둘 다 설정값).
- **하단**: 상태바(주문 접수/거부 결과)만. **미체결 목록·정정·취소는 별도 화면 `order_list`**(구현됨, 메인 메뉴 "주문 리스트") — /manual_state의 open_orders를 폴링해 표시, `manual_amend`/`manual_cancel` 사용. HL·LS 공통. **체결 내역도 같은 창에서 본다**(구현 2026-08-10, 개정) — **접수·체결을 한 표**에 두고 **유형 필터(접수/체결 체크)** 로 선택한 것만 표시. 컬럼 `구분 | 거래소(LS/HL) | 종목 | 매매 | 수량 | 가격 | 상태 | 시각`. **구분='주문'(미체결)/'체결'** — 상태(accepted 등)와 헷갈리지 않게 '접수' 대신 '주문'. **상태는 한글**(접수·부분·신규·체결·취소·거부). 시각은 별 칸(체결 행만). **전부 검은색**(사용자 확정) — 매수/매도는 매매 칸 텍스트로만 구분. 취소·정정은 접수 행만 대상(체결 행 iid `__fill*`). 체결내역은 코어가 `LiveSystem.fills`(deque, 추적 주문 체결을 종목·방향과 함께 보관, 외부 미추적은 스킵)에 담아 manual_snapshot `fills`로 노출. **정정은 LS만(확정 2026-08-11)** — 버튼 "선택 정정", 옆에 "(LS 만 정정 가능)" 안내. **HL 정정은 화면에서 차단**(선택 시 하단로그 "정정 불가 — HL은 정정 미지원(취소 후 신규)"). 이유: HL modify(batchModify)는 크로싱(즉시체결) 정정 시 **원주문을 취소하고 새 주문은 거부해 주문이 소실**된다(always_place=false가 크로싱 Gtc를 ALO로 강제 — 실측). 사전 크로싱 판정은 시세 지연·레이스로 불가. Reduce/Post 체크는 제거(HL 정정 옵션이었음).

### 1-1) [신규] 화면에 반드시 있어야 할 값 (2026-08-06)

HL 포지션 정보. 원문 표기를 그대로 두고 뜻을 붙인다.

| 화면 항목명 | 원문 표기 | 뜻 | 비고 |
|---|---|---|---|
| 수량 | 포지션수량 | 보유 수량. **매수면 + , 매도면 −** | 이미 있음 |
| 진입금액 | position value | 포지션 평가금액. **달러** | 이미 있음(평가금액) |
| 진입가 | entry price | 평균 진입단가. **소수점 2자리까지** | 이미 있음(평균단가) |
| pnl | pnl | 손익 | 이미 있음(평가손익) |
| liq.pr | liq.pr | 강제청산 가격 | 이미 있음(청산가) |
| margin | margin | 이 포지션에 잡힌 증거금 | **새로 붙여야 함** |
| Funding | funding | 보유 포지션의 **누적 펀딩 수익** | **새로 붙여야 함. 없어도 됨** |
| FundRate | funding / countdown 의 앞부분 | 지금 **펀딩률** | **새로 붙여야 함. 없어도 됨** |
| CountDown | funding / countdown 의 뒷부분 | 다음 펀딩까지 **남은 시간** | **새로 붙여야 함. 없어도 됨** |
| Oracle | oracle | 오라클 가격 | **새로 붙여야 함** |

`Margin` · `Oracle` 은 있어야 하고, 펀딩 셋(`Funding` · `FundRate` · `CountDown`)은 없어도 된다.

원문의 `funding / countdown` 은 한 칸이었는데 **화면에서 둘로 나눴다**(2026-08-06 사용자 화면안).
나누면서 앞쪽 이름이 누적 펀딩 수익(`Funding`)과 겹쳐 **`FundRate`** 로 바꿨다.

| 화면 이름 | 무엇 |
|---|---|
| `Funding` | 이 포지션을 들고 있는 동안 **쌓인 펀딩 수익** (금액) |
| `FundRate` | **지금 펀딩률** (비율) |
| `CountDown` | **다음 펀딩까지 남은 시간** |

**전부 HL이 주는 값이다. 새 API를 붙일 필요는 없다.**
`clearinghouseState` 한 번 호출이면 8개가 한꺼번에 나오는데, 지금 코드는 그 응답에서
`szi`·`entryPx` 둘만 뽑아 쓰고 나머지를 버리고 있다(`hl_live.get_positions`). **파싱만 늘리면 된다.**

| 화면 항목 | HL 응답 필드 | 어느 호출 | 지금 상태 |
|---|---|---|---|
| 수량 | `assetPositions[].position.szi` (부호 있음) | clearinghouseState | 이미 씀 |
| 진입금액 | `position.positionValue` | clearinghouseState | 버리는 중 → 뽑기만 |
| 진입가 | `position.entryPx` | clearinghouseState | 이미 씀 |
| pnl | `position.unrealizedPnl` | clearinghouseState | 버리는 중 → 뽑기만 |
| liq.pr | `position.liquidationPx` | clearinghouseState | 버리는 중 → 뽑기만 |
| margin | `position.marginUsed` | clearinghouseState | 버리는 중 → 뽑기만 |
| Funding | `position.cumFunding.sinceOpen` | clearinghouseState | 버리는 중 → 뽑기만 |
| FundRate | `assetCtx.funding` | metaAndAssetCtxs / WS | **이미 받고 있음** (`hl_live.get_funding`) |
| CountDown | 없음 — **계산해야 함** | — | HL 펀딩은 매시 정각이라 다음 정시까지 남은 시간 |
| Oracle | `assetCtx.oraclePx` | metaAndAssetCtxs / WS `activeAssetCtx` | **이미 받고 있음** (`hl.HLQuote.oracle`) |

**덤으로 같이 나오는 것들** (필요하면 그냥 쓰면 된다)

- `position.maxLeverage` — 그 종목의 레버리지 상한
- `position.leverage` {type, value, rawUsd} — **지금 걸려 있는 마진 모드와 배수**. §1-3 화면 표시에 쓴다
- `position.returnOnEquity` — 수익률
- `marginSummary` {accountValue, totalMarginUsed, totalNtlPos, totalRawUsd} — 계정 전체

**안 되는 것**

- **시장가 주문은 없다.** 지금 게이트웨이가 `mark × (1 ± 슬리피지)`로 지정가를 만들어 흉내내고 있다
  (`hl_live._market_px`). 이 창은 어차피 지정가 전용이라 문제 없다.
- **포트폴리오 마진은 API로 못 켠다.** HL 웹에서만 켜지고 거래대금 요건이 붙는다. §1-3 참조.

### 1-2) [신규] 주문가 제한 (설정, 2026-08-06)

손가락이 미끄러져 엉뚱한 가격에 주문이 나가는 것을 막는 장치.

- 매수는 `현재가 × (1 + x%)` **이상이면 주문 불가**.
- 매도는 `현재가 × (1 − x%)` **미만이면 주문 불가**.
- `x` 는 설정값(기본 **5%**). 판정은 **코어에서** 한다(화면을 우회해도 막히도록). 화면은 미리 경고만 띄운다.
- **기준가는 마크가(`markPx`)** 를 쓴다(기본값, §1-4에서 바꿀 수 있음).
  마크가는 HL이 청산·증거금 판정에 쓰는 값이라 얇은 호가에서 마지막 체결가가 튀어도 잘 흔들리지 않는다.
  게이트웨이가 이미 받고 있다(`hl_live.get_mark`).
  **화면 경고와 코어 판정은 같은 기준가를 써야 한다** — 다르면 "화면엔 괜찮은데 거부됨"이 난다.

이건 §2의 WS 건강 경고("차단 없이 경고만")와 성격이 다르다. **이쪽은 실제로 막는다.**

### 1-3) [신규] 레버리지 · 마진 모드 (2026-08-06 조사)

**주문을 낸다고 자동으로 바뀌지 않는다. 주문 전에 따로 바꿔야 한다.**

HL의 주문 액션에는 레버리지·마진모드 항목이 아예 없다. 별도 액션이다.

```
{ "type": "updateLeverage",
  "asset":    코인 인덱스,
  "isCross":  true(교차) / false(격리),
  "leverage": 정수 배수 }
```

- **코인별 설정이고 계정에 저장된다.** 한 번 바꾸면 그대로 유지되니 주문마다 부를 필요는 없다.
- 격리로 걸어둔 뒤 증거금을 넣고 빼는 것은 `updateIsolatedMargin` 이라는 또 다른 액션이다.
- 배수는 종목마다 상한이 있다(`position.maxLeverage`). 넘기면 거부된다.
- 포지션을 들고 있는 상태에서 배수를 낮추면 증거금이 모자라 거부될 수 있다.

**화면 동작 (개정 2026-08-06 — 사용자 화면안 반영).**

1. 창을 열거나 종목을 바꿀 때 `clearinghouseState` 의 `position.leverage` 를 읽어
   **레버리지 버튼 캡션에 지금 걸린 값을 쓴다** (예: `Cross  10 x`).
2. 버튼을 누르면 **설정 팝업**이 뜬다 — 교차/격리 선택 + 배수 입력 + `[적용] [취소]`.
3. `[적용]` → `updateLeverage` 호출.
   - **성공하면** 팝업을 닫고, 값을 다시 읽어 **버튼 캡션을 갱신**한다.
   - **실패하면** 팝업을 **닫지 않고** 그 자리에 사유를 보여준다
     (상한 초과, 포지션 보유 중 증거금 부족 등).
4. `[취소]` → 아무것도 하지 않고 닫는다.
5. **주문 버튼은 레버리지를 건드리지 않는다.**

**"pm" = 포트폴리오 마진 (확정 2026-08-06). 주문창에서는 못 켠다.**

후보가 셋이었고 성격이 전부 다르다.

| 후보 | 무엇 | 어느 단위 | API로 되나 |
|---|---|---|---|
| ① 격리 (isolated) | 포지션마다 증거금을 따로 묶음 | **코인별** | **된다** — `updateLeverage` {isCross:false} |
| ② dex 담보 자동이동 | HIP-3 dex 주문 시 메인 USDC 잔고에서 담보를 자동으로 끌어옴 | **계정(또는 에이전트)별** | **된다** — `userDexAbstraction` {enabled} |
| ③ 포트폴리오 마진 | 현물 계정과 perp 계정을 하나로 합침 | **계정 전체** | **안 된다** — HL 웹에서만 |

**③ 포트폴리오 마진이라면 이 창에서 켤 수 없다.**

- 거래소 액션 목록(order · cancel · updateLeverage · updateIsolatedMargin · … · userDexAbstraction)에
  **포트폴리오 마진을 켜고 끄는 액션이 없다.** HL 웹의 "Account Unification" 탭에서만 켠다.
  공식 문서와 **공식 파이썬 SDK(우리가 쓰는 것)** 양쪽을 다 뒤졌으나 `updatePortfolioMargin` 같은 이름은 없다
  (SDK에 있는 것은 `update_leverage` · `update_isolated_margin` · `user_dex_abstraction` · `user_set_abstraction`).
  **확인하는 법** — HL 웹에서 직접 켜보고 브라우저 개발자도구로 `/exchange` 요청을 잡으면 실제 액션 이름이 보인다.
  거기서 이름이 나오면 그때 붙이면 된다.
- 가입 문턱은 바뀌는 중이다. 2026-05 기준 가중 거래대금 500만 달러였고, 이후 "1만 달러 이상 계정,
  거래량 요건 없음"으로 낮춘다는 이야기가 나온다. **켤 수 있는지와 API로 켤 수 있는지는 다른 문제다.**
- 공급·차입 한도가 걸린 베타(pre-alpha → alpha)다. 한도에 닿으면 자동으로 원래 모드로 되돌아간다.
- **현물 + perp 를 합치는 기능**이라, 현물을 안 쓰는 우리 구조와는 얻는 게 별로 없다.

**그래서 이렇게 한다 (확정).**

- 주문창의 선택지는 **교차 / 격리** 만 둔다.
- 포트폴리오 마진은 **지금 켜져 있는지 보여주기만** 하고, 켜고 끄는 것은 HL 웹에서 한다.
- 나중에 실제 액션 이름이 확인되면 그때 **설정 화면**에 스위치를 붙인다. 주문창에는 넣지 않는다.

**참고 — ② dex 담보 자동이동은 별개로 쓸모가 있다.**

우리는 HIP-3(Trade.xyz) dex 의 perp 를 쓰는데, HIP-3는 **dex마다 증거금이 따로 논다**
(`hl_live.get_margin` 주석 — "HIP-3는 dex별 마진 분리"). 그래서 그 dex 로 담보를 따로 옮겨두어야 주문이 나간다.

`userDexAbstraction` 을 켜두면 **HIP-3 perp 주문을 낼 때 메인 USDC perp 잔고에서 담보를 자동으로 끌어오고,
포지션이나 미체결이 풀려 자금이 남으면 원래 자리로 돌려보낸다.** 손으로 옮길 필요가 없어진다.

```
{ "type": "userDexAbstraction",
  "hyperliquidChain": "Mainnet",
  "signatureChainId": "0xa4b1",
  "user": "0x...",
  "enabled": true,
  "nonce": 밀리초 }
```

이것도 계정 단위 설정이라 **설정 화면에 둔다.** 주문마다 바꿀 값이 아니다.
'pm' 과는 별개 항목이니, 필요하면 따로 결정한다.

### 1-4) [신규] 기본값 — 설정으로 바꿀 수 있는 것들 (2026-08-06)

아래는 **지금 정해 둔 기본값**이다. 전부 설정 항목이라 **바꿔도 코드 구조는 그대로다.**
운영하면서 편한 값으로 조정하면 된다.

| 항목 | 기본값 | 지금 코드 | 비고 |
|---|---|---|---|
| 호가창 표시 단수 | **5호가** | 8호가 (`order_hl._fill_hoga` 의 `[:8]`) | 매도 5줄 + 매수 5줄 |
| 원클릭 안전잠금 | **켜짐** | 없음 | 체크돼야만 발주(팝업 없음). 해제 시 발주 안 하고 로그 안내 |
| 호가창 갱신 주기 | **500 ms** | 400 ms (`_reschedule(refresh, 400)`) | 사실상 이미 만족 |
| 주문가 제한 폭 | **5 %** | 없음 | §1-2 |
| 주문가 제한 기준가 | **마크가(`markPx`)** | 화면은 마지막 체결가(`_ref_price`) | 마지막 체결가 · 오라클가로도 바꿀 수 있게 |
| 레버리지 | **5 배** | 없음 | 종목별 상한(`maxLeverage`) 안에서 |
| 마진 모드 | **교차(cross)** | 없음 | §1-3 |

설정값은 다른 화면과 같이 `core_state.json` 에 저장했다가 다시 띄울 때 불러온다.

### 1-5) [신규] 잔고표 갱신 방식 (2026-08-06)

**실시간으로 갱신된다. 조회 버튼은 따로 두지 않는다.**

- 화면 뒷단 스레드가 `/manual_state` 를 **0.5초마다** 읽어 표를 다시 그린다(§1-4).
- 코어는 OrderBook 으로 잔고를 실시간 유지한다 — 최초 한 번 REST 로 받고,
  그 뒤로는 체결통보로 증분 반영한다. **조회를 반복하지 않는다.**

**다만 한 군데 구멍이 있다.**

OrderBook 은 **우리 시스템으로 낸 주문의 체결만** 반영한다.
HL 웹이나 다른 데서 거래하면 이 화면에 안 잡힌다.
원래 '적' 버튼이 그때 재조회를 해주는 역할이었는데, **'적' 을 없애면 재동기 수단이 사라진다.**

**'적' 버튼이 그 역할을 한다.**

같은 종목에서 '적'을 다시 누르면 `manual_refresh` 가 나가 잔고를 다시 읽어온다.
종목 확정과 잔고 재동기를 **버튼 하나가 겸하므로 별도 새로고침 버튼은 두지 않는다.**

밖에서 거래했다 싶으면 '적'을 한 번 누르면 된다.
잔고표 옆에 **마지막 갱신 시각**을 작게 찍어두면 값이 언제 적 것인지 바로 보인다.

### 2) 도메인 불변식·가드 (§3 강제)

- **공매도 금지 = 보유 초과 매도 금지**(전량 차단 아님 — 보유분 매도는 청산이라 정상): 코어가 **OrderBook(§5.9)** 으로 잔고를 실시간 유지(최초 REST 스냅샷 + 실시간 체결통보 SC* 증분, 조회 폴링 없음). LS 주식 매도 주문 시 코어가 **매도가능수량 = OrderBook 보유수량 − 미체결 매도수량**을 즉시(재조회 없이) 계산해, 주문수량이 이를 넘으면 거부(사유: 공매도 금지). 화면은 이 값을 표시하고 초과 입력 시 경고(이중 안전 — 화면 우회해도 코어가 막음). **주의**: OrderBook은 우리 시스템으로 낸 주문의 체결만 반영 — HTS 등 외부 거래는 안 잡히니, **창 열 때 + '잔고 새로고침' 버튼**으로 `get_positions` 재조회해 재동기(주문마다가 아니라 필요할 때만). LS 주식선물·HL perp는 양방향 허용(숏 가능).
- **계좌 라우팅**: `routing.account_for`로 결정(주식→KR_STOCK, 주식선물→KR_DERIV, HL→HL). 화면은 관여 안 함.
- **세션 데드존**: 수동 주문은 사람이 판단하므로 데드존/세션 가드로 **막지 않는다**(닫혀 있으면 LS/HL가 거부). 자동 판정 루프(§6.2)와 무관.
- **WS 건강 경고(Phase 8-6, 확정 2026-08-05 / 표시 위치 개정 2026-08-10)**: WS 끊김/시세 무데이터(N초, env `KP_WS_MAX_IDLE_S` 기본 10초)여도 **수동 주문은 막지 않고 발주하되, 응답 `warnings`에 사유를 싣는다**(사용자 확정: "차단 없이 경고만"). 판정은 순수 함수 `ws_status.order_block_reason`. **표시 위치(개정)**: 이 경고는 **주문창 하단 로그에는 표시하지 않는다** — 그 로그는 **주문 관련(성공/거부/접수)만** 보여준다(주문 성공=검정, 거부·실패=빨강). WS 건강 상태는 **메인창 WS 세션 표**에서 본다(사용자 확정 2026-08-10: HL 주문에 무관한 LS 무데이터 경고가 붙어 거부처럼 보이는 혼동 제거). 코어는 `warnings`를 계속 실어 보내므로 다른 소비자·자동 발주 게이트는 영향 없음. **자동 발주(§6.2, 추후)는 같은 게이트로 하드 차단**(신규 진입 금지 = 데드존 불변식).

- **정정은 LS만(개정 2026-08-11)**: **수동 정정(order_list)은 LS 주문만** 허용한다. HL은 화면에서 차단(선택 시 "정정 불가 — 취소 후 신규"). 이유: HL modify(batchModify)는 **크로싱 정정 시 원주문 취소+새주문 거부로 주문 소실**(always_place=false → 크로싱 Gtc를 ALO 강제, 실측 로그 확인). 사전 크로싱 판정은 시세 지연·레이스로 신뢰 불가라 안 한다. Reduce/Post 옵션 UI 제거. **주의**: peg_order의 HL 자동 페깅은 별개(대기 가격이라 크로싱 드묾, 추후 재검토). 백엔드 `amend_price`/`HLSdkGateway.amend_order`(modify)는 유지(peg 사용).

### 3) 수량 단위

수동은 **다리 하나씩**이라 그 거래소 **네이티브 단위**로 입력(자동의 1:10 페어 환산과 다름): LS 주식=주, LS 주식선물=계약, HL=계약. ('세트'·페어 환산 없음 — 페어 수량 규칙은 §6.2-3 자동 전용.)

### 4) 코어 신규 명령 (화면 → 코어, `POST /command`)

- `manual_order` {instrument, underlying, side, order_type(=limit), qty, price, reduce_only?, post_only?} → 공매도·라우팅 검증 후 `place`, `order_id`. reduce_only/post_only는 **HL 전용**(LS는 무시).
  **[신규]** 레버리지·마진모드는 **여기서 받지 않는다** — 주문과 별개 액션이라 아래 `manual_leverage` 로 뺀다.
- `manual_amend` {order_id, price} → `amend_price`(잔량 기준 정정 = 취소+신규).
- `manual_cancel` {order_id} → `cancel`.
- `manual_hl_merge` {underlying, n_sig_figs, mantissa} → `set_hl_aggregation`(HL 호가단위 머지, WS 재구독).
- `manual_refresh` {} → `refresh_snapshot`(잔고/포지션 재조회 → OrderBook 재동기, '적' 버튼·HTS 외부거래 반영).
- **[신규]** `manual_leverage` {underlying, is_cross, leverage} → `updateLeverage`. **주문과 별개** (§1-3).
- **[신규·나중]** `hl_dex_abstraction` {enabled} → `userDexAbstraction`. **계정 단위라 주문창이 아니라 설정 화면에 둔다.** 이 창의 개발 범위 밖.
- **[신규]** `manual_price_guard` {pct} → 주문가 제한 폭(§1-2) 설정.
- `GET /manual_state` (별도 엔드포인트 — /state를 무겁게 안 하려 분리): 취급 종목별 **호가 사다리·포지션·평균단가·평가손익·평가금액·청산가·매도가능·잔고·틱** + **전체 미체결**. OrderBook·`quotes` 메모리 읽기(조회 폴링 없음). 화면 뒷단 스레드가 폴링해 표시.
  **[신규]** `position_value` · `unrealized_pnl` · `margin_used` · `cum_funding` · `oracle` · `funding_countdown`
  + `leverage` {type, value} · `max_leverage` 추가 (§1-1, §1-3). 전부 `clearinghouseState` 한 번으로 나온다.

### 5) 확인 이력 (전부 닫힘 — 2026-08-06)

| 항목 | 결과 |
|---|---|
| 'pm' 이 무엇인지 | **포트폴리오 마진.** API로 못 켠다(HL 웹 전용). 주문창에는 교차/격리만 두고 상태 표시만 한다. §1-3 |
| 레버리지 변경 시점 | **주문과 별개 액션.** 바꾸고 나서 주문해야 한다. 코인별로 계정에 저장된다. §1-3 |
| margin · oracle · funding 출처 | **전부 HL이 준다.** 8개는 `clearinghouseState` 한 번, oracle 은 이미 WS로 받는 중. §1-1 |
| "5호가 0.5초" 의 뜻 | 표시 5단수 + 500ms 갱신으로 정함. **둘 다 설정값.** §1-4 |
| 주문가 제한 기준가 | **마크가(`markPx`).** 설정으로 바꿀 수 있음. §1-2 · §1-4 |

**이 문서로 개발을 시작할 수 있다.** 남은 미결은 없다.
아래 §6은 이 창의 다음 판(v2) 이야기라 지금 개발 범위가 아니다.

### 6) v2 [OPEN] (이후)

마크가 기반 최대수량·증거금 자동계산, 예상체결가(est-pr) 표시, 시장가.
(레버리지는 v1으로 올라감 — §1 개정 2026-08-06)
