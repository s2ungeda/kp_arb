# 자동M 발주 흐름 — 정방향 진입·청산 상세 (체결쏴)

> **문서 성격:** 초안(작성 중)이며 **계약(spec)의 일부**다. `DESIGN-auto-m.md`의 실행 규칙(§4~§9)을 "언제 어떤 상황이면 무엇을 한다"까지 풀어 쓴 것이다.
> **왜 중요한가(사용자 강조 2026-08-27):** 여기서 정한 상태·상황·처리 흐름은 자동M 하나만을 위한 것이 아니다. **앞으로 만들 다른 전략들도 대부분 같은 흐름**(먼저 국내에 걸고, 걸린 게 체결되면 반대편 해외를 즉시 잡는 방식)을 따른다. 그래서 이번에 뼈대를 제대로 세워두면, 새 전략은 "무슨 상품을, 어느 방향으로, 어떤 조건에서" 부분만 바꿔 끼우면 된다.
> **다루는 범위:** **정방향 진입**(§4·§5·§6)과 **정방향 청산**(§7)을 끝까지 파고든다. 역방향(진입·청산)은 좌우가 뒤집힌 대칭이라 같은 뼈대를 재사용하며, 확정 후 이어서 붙인다(§11).

---

## 0. 용어와 공통 뼈대

**두 다리 한 쌍으로 움직인다.** 자동M은 국내 주식선물(SF) 한 자리와 해외 HL 무기한선물 한 자리를 짝지어, 둘의 가격차(괴리)가 벌어질 때 들어가고 좁혀질 때 빠져나온다.

- **선주문** = 국내 SF에 **먼저** 내는 주문. **지정가로 호가에 걸어두고 체결을 기다리는 방식**(maker). 우리가 값을 정한다.
- **후주문** = 선주문이 체결된 뒤 **곧바로** 내는 HL 주문. **즉시 체결시키는 방식**(taker). 선주문 체결 수량의 10배(SF 1계약 = HL 10계약).
- **예상체결가(est)** = 지금 이 수량을 실제로 밀어넣으면 평균 몇 원에 체결될지 호가창을 훑어 계산한 값.
- **역산가** = "이 괴리 기준값을 만족시키려면 선주문을 얼마에 걸어야 하는가"를 거꾸로 푼 값(§5).

**한 판의 흐름(한 줄):** 조건을 통과하면 → 선주문을 호가에 건다 → 체결되면 → 후주문을 즉시 낸다 → 양쪽 다 체결됐는지 확인한다 → 잠깐 쉬었다가(딜레이) → 다시 처음으로. 어딘가 어긋나면(헤지가 깨지면) 즉시 **멈추고 알린다**.

**전략마다 바뀌는 것 / 전략이 공유하는 것:**

| 전략마다 바꿔 끼우는 것 | 모든 전략이 함께 쓰는 뼈대(이 문서) |
|---|---|
| 상품(주식선물·주식·ETF …), 사고팔 방향 | 상태 목록(§2) |
| 판정 조건(어떤 괴리를 어느 부등호로 볼지) | 일어나는 일(신호) 목록(§3) |
| 역산 공식·기준가·발주 허용범위 | 상태가 어떻게 넘어가는지(§4~§7) |
| 후주문 수량 배수(SF는 10배) | 실패·거부·헤지 어긋남·멈춤 처리 |

---

## 1. 정방향 진입이란

**정방향**은 "HL이 비싸고 국내가 싼" 국면을 노린다. 이때 **싼 국내를 사고(선주문 SF 매수) 비싼 HL을 판다(후주문 HL 매도).** 나중에 둘의 차이가 좁혀지면 청산(§7)에서 반대로 되돌려 차익을 남긴다.

- **선주문:** 국내 SF **매수**를, 우리가 정한 값에 호가로 건다.
- **후주문:** 선주문이 체결된 만큼 HL을 **매도**로 즉시 잡는다(체결 수량 × 10계약).
- **들어가는 조건:** 국내선물 괴리와 국내현물 괴리가 **둘 다** 기준을 넘어야 한다(§4 G5).

---

## 2. 상태 목록 (세트 한 개 기준)

한 세트는 아래 일곱 상태 중 하나에 있다. 3세트·정역은 각각 따로 돈다(§4 G-동시성).

| 상태 이름 | 코드값 | 무슨 상황인가 |
|---|---|---|
| **대기** | `IDLE` | 진입 실행을 껐거나, 조건이 안 맞아 아무 주문도 없는 상태 |
| **감시** | `ARMED` | 진입 실행을 켠 상태. 시세가 들어올 때마다 §4 조건을 하나하나 따진다 |
| **선주문대기** | `PRE_RESTING` | 선주문(국내 SF)을 걸어두고 체결을 기다리는 중 |
| **부분체결** | `PRE_PARTIAL` | 선주문이 일부만 체결됨. 체결된 만큼은 후주문으로 헤지하고, 남은 수량은 계속 대기 |
| **후주문대기** | `POST_PENDING` | 후주문(HL)을 즉시로 내고 체결 확인을 기다리는 중 |
| **딜레이대기** | `SETTLE_DELAY` | 한 판이 끝나 다음 선주문까지 잠깐 쉬는 중(선주문 딜레이) |
| **중지** | `HALTED` | 헤지가 깨져 멈춘 상태(후주문 거부로 인한 체결차, 또는 체결차 감지) + 소리 알림. **사람이 직접 풀어야 재개.** 선주문 거부는 여기 해당 없음(잠깐 쉬었다 다시 냄) |

---

## 3. 일어나는 일(신호) 목록

상태를 바꾸는 계기들이다.

시세 들어옴 · 조건 충족/미충족 · 선주문 일부체결 · 선주문 전부체결 · 선주문 거부 · 후주문 전부체결 · 후주문 거부 · 역산가 바뀜 · 발주범위 벗어남 · 체결차 생김 · 실행 켬/끔 · 주문시간 끝남 · 전환대기 끝남 · **시장 정지/재개**(§8)

---

## 4. 발주 통과 조건 (감시 상태에서 시세마다, 순서대로)

진입 실행을 켜면 세트는 **감시** 상태로 들어가, 시세가 올 때마다 아래 여섯 조건을 **차례로** 따진다. **하나라도 걸리면 선주문을 내지 않고, 이미 걸어둔 게 있으면 대개 취소**한다. 여섯을 다 통과해야 선주문을 건다.

| 순서 | 조건 | 통과 못하면 |
|---|---|---|
| G1 | 진입 실행이 켜져 있는가 | 대기 상태로 |
| G2 | 지금이 주문 가능한 시간대(2구간) 안인가 | 안 냄 + 걸어둔 미체결 선주문 **취소** |
| G3 | 전환대기가 끝났는가(직전 청산 체결 뒤 N초 지났는가) | 안 냄 |
| G4 | 더 들어갈 여유가 있는가 — 이번에 낼 계약수 = `Min(1회주문수량, 목표수량 − RT선진입)` 이 **1 이상**인가 | 안 냄(목표를 다 채운 상태). 청산으로 RT가 줄면 다시 열림(§9) |
| G5 | **판정:** 국내선물 괴리 > `-HP/+SF` **그리고** 국내현물 괴리 > `-HP/+S` (둘 다여야 함) | 걸어둔 선주문 있으면 **취소** |
| G6 | **역산가가 발주 허용범위 안인가** — 역산가 계산 → 호가단위로 내림 → 허용 하한(한계) 이상인가 | 안 냄 / 걸어둔 선주문 **취소** |

**여섯 다 통과 →** 선주문(국내 SF 매수, 계약수만큼, 역산가에) 발주 → **선주문대기**로. 이미 같은 선주문이 걸려 있으면 그대로 두되, **역산가가 바뀌었으면 정정**한다(§5·§6).

### G5·G6을 풀어 쓰면

1. **먼저 G5(판정).**
   - 두 괴리 중 **하나라도 기준 미달**이면 → 걸어둔 선주문이 있으면 취소하고, 새로 내지 않는다.
   - 둘 다 넘으면 → G6으로 간다.
2. **다음 G6(역산 → 허용범위).**
   - 역산가를 구하고(§5), 국내 SF 호가단위로 **내림**한다(매수라 유리하게).
   - 그 값이 **허용 하한(한계)보다 낮으면**(= 너무 싸게 사려고 물러나 있어 체결 가망이 낮으면) → 안 내고, 걸어둔 게 있으면 취소.
   - 허용 하한 이상이면 → 발주(또는 값이 달라졌으면 정정).
   - 이 계산은 **시세가 올 때마다 다시** 한다. 그래서 걸어둔 선주문도 시세가 움직이면 역산가를 새로 구해 재점검하고, 값이 바뀌면 정정한다.

> **실예 (숫자로).** 하이닉스 선물이론가 200,000원, 진입SF 기준값 0.5%, HL 예상체결 괴리 1.0%, 선주문 호가단위(틱) 3,000원, 발주범위 0.4%, 국내 SF 매도1호가 201,500원이라 하자.
> - **역산가** = 200,000 × (1 + 1.0% − 0.5%) = 200,000 × 1.005 = **201,000원** (틱 3,000의 배수라 내림해도 201,000).
> - **허용 하한(한계)** = (매도1호가 − 1틱) × (1 − 범위) = (201,500 − 3,000) × (1 − 0.4%) = 198,500 × 0.996 = **약 197,706원**.
> - 역산가 201,000 ≥ 한계 197,706 → **선주문을 201,000원에 매수로 건다.** (매도1호가 201,500보다 낮으니 바로 체결되지 않고 호가에 걸린다.)
> - 만약 시세가 밀려 역산가가 197,000원까지 내려가면(한계 미달) → 그 선주문은 **취소**한다.

---

## 5. 역산가와 발주 허용범위 (G6 상세)

- **역산가** `P = 선물이론가 × (1 + HL예상체결괴리 − 기준값)`. HL예상체결괴리는 후주문 수량(계약수×10)만큼 HL 매수호가창을 훑은 값(자세히는 `DESIGN-auto-m.md §6`).
- **호가단위 내림:** 매수는 싸게 살수록 유리하니, 국내 SF 선주문 호가단위(하이닉스 3,000·삼성 500·현대차 1,000)로 **내림**한다.
- **발주 허용범위(한계):** 매수는 `한계 = (상대 매도N호가 − 1틱) × (1 − 범위)`. 역산가가 이 값 **이상**이어야 낸다. 여기서 **상대 매도N호가는 그 시점 호가창의 실제 N번째 매도호가**(공통설정 콤보에서 1~5 선택). 빈 호가는 건너뛴다.

---

## 6. 상태가 넘어가는 규칙 (정방향 진입)

각 상태에서 어떤 일이 생기면 어떻게 처리하고 어디로 가는지다. **§4를 통과해 선주문을 건 뒤부터**를 다룬다.

### 선주문대기 (`PRE_RESTING`) — 시세마다 재점검

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| G2·G5·G6 중 하나 깨짐(시간 끝·조건 미달·범위 벗어남) | 선주문 **취소** | 감시/대기 |
| 역산가 바뀜 | **정정**: 취소 → 딜레이 → 새 값으로 다시 걺 | 선주문대기 |
| 실행을 끔 | 선주문 **취소**(이미 체결된 포지션은 그대로 둠) | 대기 |
| 선주문 일부 체결 | — | 부분체결 |
| 선주문 전부 체결 | 후주문(HL 매도, 체결수량×10) 즉시 발주 | 후주문대기 |
| 선주문 거부 | **딜레이 뒤 다시 냄**(아직 체결 전이라 위험 없음 — 멈추지 않음) | 감시 |

### 부분체결 (`PRE_PARTIAL`) — 체결된 만큼 즉시 헤지, 남은 건 계속 대기

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 일부 체결됨 | 체결된 수량 × 10을 HL 매도로 **즉시** 냄(헤지 지연 최소) | 후주문대기 병행 |
| 역산가 바뀜 | **남은 선주문만 정정**(취소→딜레이→다시 걺). 이미 체결·헤지된 부분은 그대로 | 부분체결 |
| 남은 수량 전부 체결 | 그 몫 후주문 발주 | 후주문대기 |
| 남은 수량 거부 | 딜레이 뒤 다시 냄(체결된 부분은 이미 헤지됨) | 감시 |

> **실예.** 선주문 10계약을 걸었는데 4계약만 체결됐다. → 즉시 HL 40계약을 매도로 낸다(4×10). 남은 6계약은 그대로 호가에 걸려 있다. 이때 시세가 움직여 역산가가 바뀌면, **남은 6계약만** 취소하고 새 값으로 다시 건다. 이미 잡은 4계약(SF)+40계약(HL)은 손대지 않는다.

### 후주문대기 (`POST_PENDING`) — 후주문은 즉시 체결이라 부분·재호가 없음

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 후주문 전부 체결 | **RT선진입 += 선주문 SF 체결계약수**(헤지 완성), 누적 결과 갱신(§10), 체결차 0 확인 | 딜레이대기 |
| 후주문 거부 | 국내는 체결됐는데 HL이 안 잡힘 → **체결차 발생 → 중지 + 알림**(다시 안 시도) | 중지 |

### 딜레이대기 (`SETTLE_DELAY`)

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 선주문 딜레이(ms) 지남 | 다음 판정으로 | 감시 |

### 어느 상태에서든 끼어드는 일

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 체결차 생김(방향별 SF잔고×10 + HL잔고 ≠ 0) | **중지 + 알림**, 추가·재주문 안 함, 사람이 직접 정리. 헤지가 깨진 세트는 멈춤(후주문 거부가 대표) | 중지 |
| 실행을 끔 | 걸어둔 미체결 선주문 **취소**(체결 포지션은 유지) | 대기 |

> 알림 방법(소리·팝업 등)은 **추후 확정**(사용자 2026-08-27). 지금은 "중지 + 알림"으로만 적는다.

---

## 6A. 경우의 수 — 정방향 진입 (상황별로 전부 나열)

위 규칙을 "이런 상황이면 이렇게"로 다시 펼친 것이다. 진입에서 나올 수 있는 경우를 단계별로 모두 적는다.

### ㄱ. 선주문을 낼지 판정할 때 (감시)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄱ1 | 조건 통과 + 역산가가 허용범위 안 | 선주문(SF 매수, 계약수=Min(1회주문, 목표−RT))을 역산가에 걺 | 선주문대기 |
| ㄱ2 | 두 괴리 중 하나라도 미달 | 안 냄(걸어둔 선주문 있으면 취소) | 대기 |
| ㄱ3 | 조건은 맞는데 역산가가 허용 하한보다 낮음(너무 물러남) | 안 냄(있으면 취소) | 대기 |
| ㄱ4 | 지금이 주문 가능 시간대가 아님 | 안 냄 + 미체결 선주문 취소 | 대기 |
| ㄱ5 | 전환대기가 안 끝남(직전 청산 체결 뒤 N초 이내) | 안 냄 | 대기 |
| ㄱ6 | 목표를 다 채움(RT=목표라 이번 계약수가 0) | 안 냄 | 대기(청산으로 RT 줄면 다시 열림) |
| ㄱ7 | 목표까지 남은 양이 1회주문보다 적음(목표−RT < 1회주문) | 계약수를 남은 양으로 **줄여** 발주 | 선주문대기 |

> **실예 (ㄱ7).** 목표 100계약, 이미 95계약 진입(RT=95), 1회주문 10계약. → 이번엔 10이 아니라 `Min(10, 100−95)=5`계약만 낸다.

### ㄴ. 선주문을 걸어두고 기다릴 때 (선주문대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄴ1 | 역산가가 바뀜 | 취소 → 딜레이 → 새 값으로 다시 걺 | 선주문대기 |
| ㄴ2 | 조건이 미달로 바뀜 | 선주문 취소 | 감시 |
| ㄴ3 | 역산가가 허용범위를 벗어남 | 선주문 취소 | 감시 |
| ㄴ4 | 주문 가능 시간대가 끝남 | 선주문 취소 | 대기 |
| ㄴ5 | 선주문이 거부됨 | 딜레이 뒤 다시 냄(멈추지 않음) | 감시 |
| ㄴ6 | 선주문이 일부만 체결됨 | 체결분×10 후주문 즉시 발주 | 부분체결 |
| ㄴ7 | 선주문이 전부 체결됨 | 후주문(전량×10) 발주 | 후주문대기 |

### ㄷ. 선주문이 일부만 체결됐을 때 (부분체결)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄷ1 | 역산가가 바뀜 | **남은 수량만** 정정. 헤지된 부분은 그대로 | 부분체결 |
| ㄷ2 | 남은 수량이 전부 체결됨 | 그 몫 후주문 발주 | 후주문대기 |
| ㄷ3 | 남은 수량이 거부됨 | 딜레이 뒤 다시 냄(체결분은 이미 헤지) | 감시 |

### ㄹ. 후주문을 내고 확인할 때 (후주문대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄹ1 | 후주문 전부 체결 | RT += SF 체결계약수, 누적 갱신, 체결차 0 확인 | 딜레이대기 |
| ㄹ2 | 후주문 거부 | 체결차 발생 → **중지 + 알림**(다시 안 시도) | 중지 |

> **실예 (ㄹ1).** 선주문 10계약(SF 매수)이 다 체결되고 후주문 HL 100계약(매도)도 다 체결됐다. → RT선진입이 10계약 늘고, 누적 매도수량·평균환율·실현 스프레드를 갱신한 뒤, 선주문 딜레이만큼 쉬고 다음 판정으로 돌아간다.

### ㅁ. 쉬었다가 반복 (딜레이대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㅁ1 | 후주문 전부 체결 후 | 선주문 딜레이(ms)만큼 대기 → 다음 판정 | 감시 |

### ㅂ. 예외·실행 끄기 (어느 상태에서든)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㅂ1 | 체결차 생김(방향별 SF잔고×10 + HL잔고 ≠ 0) | 중지 + 알림, 사람이 정리 | 중지 |
| ㅂ2 | 실행을 끔 | 미체결 선주문 취소, 체결 포지션은 유지 | 대기 |

### ㅅ. 여러 개가 동시에 (동시성)

| # | 상황 | 처리 |
|---|---|---|
| ㅅ1 | 3세트가 동시에 조건 통과 | 세트마다 **따로** 위 흐름을 밟는다(서로 간섭 없음) |
| ㅅ2 | 정방향·역방향이 동시에 성립 | 방향마다 **따로** 돈다(서로 다른 상품이라 간섭 없음) |

---

## 7. 경우의 수 — 정방향 청산 (상황별로 전부 나열)

**청산은 진입을 되돌리는 것.** 정방향 청산은 국내 SF **매도**(들고 있던 롱을 판다)를 먼저 걸고, 체결되면 HL을 **매수**(들고 있던 숏을 되산다)로 즉시 잡는다. 상태·신호·흐름은 진입과 똑같고, **아래만 좌우가 뒤집힌다.**

### 진입과 다른 점 (한눈에)

| 항목 | 정방향 진입 | 정방향 청산 |
|---|---|---|
| 전환대기(G3) | 직전 **청산** 체결 뒤 N초 | 직전 **진입** 체결 뒤 N초 |
| 이번 계약수(G4) | Min(1회주문, **목표−RT**) | Min(1회주문, **RT선진입**) — 즉 RT가 0보다 커야 함 |
| 판정(G5) | 선물괴리 > 기준 **그리고** 현물괴리 > 기준 | **선물괴리 < 기준(`+HP/-SF`)** — 현물은 안 봄 |
| 역산·범위(G6) | HL **매수**호가창, **내림**, 역산가 **≥ 한계**((상대매도−1틱)(1−범위)) | HL **매도**호가창, **올림**, 역산가 **≤ 한계**((상대매수+1틱)(1+범위)) |
| RT선진입 | 후주문 체결 시 **늘어남** | 후주문 체결 시 **줄어듦** |
| 누적 표기 | `-HP`/`+S`/`-환` | `+HP`/`-S`/`+환` |

> **왜 반대인가.** 진입 때 국내를 샀으니(롱) 청산은 판다. 살 때는 싸게 살수록 좋아 호가단위를 내리고 "역산가가 하한 이상"이면 걸지만, 팔 때는 비싸게 팔수록 좋아 호가단위를 **올리고** "역산가가 상한 이하"면 건다.

### ㄱ. 선주문을 낼지 판정할 때 (감시)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄱ1 | 선물괴리 < 기준 + 역산가가 청산 허용범위 안(≤ 한계) | 선주문(SF 매도, 계약수=Min(1회주문, RT))을 역산가에 걺 | 선주문대기 |
| ㄱ2 | 선물괴리 ≥ 기준(미달) | 안 냄(걸어둔 선주문 있으면 취소) | 대기 |
| ㄱ3 | 조건은 맞는데 역산가가 상한보다 높음(너무 물러남) | 안 냄(있으면 취소) | 대기 |
| ㄱ4 | 주문 가능 시간대가 아님 | 안 냄 + 미체결 취소 | 대기 |
| ㄱ5 | 전환대기가 안 끝남(직전 진입 체결 뒤 N초 이내) | 안 냄 | 대기 |
| ㄱ6 | 청산할 물량이 없음(RT=0) | 안 냄 | 대기(진입으로 RT 늘면 다시 열림) |
| ㄱ7 | RT가 1회주문보다 적음 | 계약수를 RT로 **줄여** 발주 | 선주문대기 |

> **실예 (ㄱ7).** RT선진입 3계약, 1회주문 10계약. → 이번엔 `Min(10, 3)=3`계약만 청산으로 낸다(들고 있는 만큼만 되돌린다).

### ㄴ~ㅅ. 나머지

진입(§6A ㄴ~ㅅ)과 상태·처리가 같고 방향만 반대다.

- **ㄴ(선주문대기)·ㄷ(부분체결):** 선주문 = SF 매도, 후주문 = HL 매수. 역산가 바뀌면 정정, 조건 미달·범위 이탈·시간 끝나면 취소, 거부되면 딜레이 뒤 다시 냄, 일부 체결되면 그만큼 HL 매수로 즉시 헤지.
- **ㄹ(후주문대기):** 후주문(HL 매수) 전부 체결 → **RT선진입 −= SF 체결계약수(줄어듦)**, 누적(`+HP`/`-S`/`+환`) 갱신, 체결차 0 확인 → 딜레이대기. 후주문 거부 → 체결차 → 중지+알림.
- **ㅁ(딜레이)·ㅂ(예외·끄기)·ㅅ(동시성):** 진입과 완전히 동일.

---

## 8. 시장 상황 — 정지와 재개 (사이드카·서킷브레이커·VI·거래정지)

지금까지는 "정상적으로 접속매매가 도는" 시장을 전제했다. 실제로는 국내 시장이 여러 이유로 **잠깐 멈췄다 풀리는** 일이 있다. 이 구간을 다루지 않으면, 멈춘 사이 엉뚱한 주문을 내거나 헤지가 어긋날 수 있다.

> **핵심 위험(꼭 짚을 것):** 국내(SF·현물)가 멈춰도 **HL은 24시간 계속 거래된다.** 그래서 국내만 멈춘 동안에는
> - 새 진입/청산을 **낼 수 없고**(국내 다리를 못 잡으니 한쪽만 잡히면 헤지가 깨진다),
> - 이미 잡아둔 포지션은 **HL 쪽만 값이 계속 움직인다**(체결 불일치는 아니지만 평가손익이 흔들린다).
> 따라서 국내 정지 신호가 오면 **국내 신규·정정 발주를 즉시 멈추는 것**이 안전의 기본이다.

**감지 방법(확정 2026-08-27):** 시간을 하드코딩하지 않고, **LS `JIF`(장운영정보) 실시간 웹소켓 구독**으로 장운영 상태(거래 정상·정지·단일가·VI·사이드카·서킷브레이커 등)를 실시간으로 받아 **세션(§DESIGN 세션 주도)**에 반영한다. 세션이 "거래 불가"를 알리면 데드존(신규 발주 금지)으로 취급한다. — 구체 필드·상태코드 매핑은 **LS `JIF` 문서 기준으로 확정**(추측 금지). (HL은 24시간 거래라 이런 정지 개념이 없어, 이 감지는 국내 다리 전용.)

### 정지 처리 모델 (확정 2026-08-27)

JIF `jstatus`는 성격이 셋으로 갈리고, **`jangubun`(시장)에 따라 같은 번호도 뜻이 다르다**(예: 61이 주식은 서킷1발동, 선물은 당일 장종료).

- **시간대 코드** → `SessionPhase`(장전동시호가·장시작·장마감·프리·애프터 등).
- **정지 발동/해제 코드** → **시장별 "정지 사유 오버레이"**(phase와 별개로 얹는다). 발동이면 사유 설정, 해제면 해제.
- **변동성 확대 코드(선물 70~77)** → **거래 계속**(정지 아님, 정보로만).

**핵심 규칙:**
- **발주 가능 = 시간대(phase)가 거래시간 AND 그 시장에 정지 오버레이 없음.** 정지 오버레이가 있으면 데드존과 같게(신규 발주 금지 + 미체결 취소).
- **시장별로 따로 본다.** 자동M은 선물(`jangubun 5`) 오버레이만 본다 → **주식 사이드카(`jangubun 1`, 64/66)는 자동M 발주를 막지 않는다**(결정 3). 자동T(주식)는 주식 오버레이를 본다.
- 발동/해제는 **짝**이다 — 해제 코드를 반드시 매핑해야 정지에서 풀린다.

**jstatus 분류(주요):**
- 정지 발동(주식 1,2): 61·68 서킷1·2발동, 69 서킷3발동(+당일종료), 63 서킷1 동시호가종료, 64·66 사이드카 매도·매수발동.
- 정지 해제(주식): 62·70 서킷 해제(호가접수개시), 65·67 사이드카 해제.
- 정지 발동/해제(선물 5): 63 서킷(장중동시마감)=발동, 62 서킷 해제, 61 당일 장종료(=DEAD).
- 변동성(선물 70~77): 거래 계속(정보).
- (세부 코드값은 LS `JIF` 코드표 그대로 — 구현 시 매핑 표로.)

### 상황별 처리

| 상황 | 무엇인가 | 국내 다리(SF) | 자동M 처리(제안) |
|---|---|---|---|
| **사이드카** | 선물이 급변(±5~6%)해 **프로그램매매 호가효력이 5분 정지** | 프로그램매매로 분류되면 신규·정정 5분 정지 | 국내 신규·정정 **중단**. 걸어둔 미체결 선주문 취소. HL은 손대지 않음. 5분 뒤 자동 재개 |
| **서킷브레이커(매매중단)** | 지수 급락 단계별 — 1단계 −8%(20분 정지+단일가 재개), 2단계 −15%, 3단계 −20%(당일 종료) | 전 종목 매매 정지 | 국내 신규·정정·취소 모두 **중단**(정지 중엔 취소도 불가할 수 있음). 재개 시 정상 판정 복귀 |
| **VI(변동성완화장치)** | 개별종목 급변 시 **2~10분 단일가매매**로 전환(접속매매 정지) | 접속매매 정지, 단일가만 | 그 종목 신규·정정 **중단**(호가에 걸어두는 방식이 단일가에선 무의미). 해제되면 복귀 |
| **개별 거래정지** | 공시·이상급등 등으로 그 종목 거래 정지 | 거래 정지 | 그 종목 세트 **중단**, 걸어둔 선주문 취소. 재개까지 대기 |
| **장 시작/마감 동시호가** | 접속매매가 아닌 단일가 시간대 | 단일가 | 주문가능시간(§4 G2)에서 이미 제외(2구간이 접속매매 시간이라야 함) |

### 재개(풀림) 처리 (확정 2026-08-27)

- 정지가 풀려 **접속매매로 돌아오면**, 세트는 다시 **감시**부터 정상 판정을 밟는다.
- **재개 딜레이:** 재개 직후는 단일가→접속매매 전환으로 변동이 크므로, **바로 발주하지 않고 "재개 딜레이(초)"만큼 쉰 뒤** 판정을 시작한다. **공통설정창에 "재개 딜레이(초)" 항목 신설, 기본값 10초.**
- **정지 중 포지션은 그대로 유지.** 국내가 오래(서킷 20분+) 멈춰 HL만 값이 움직여도, 이미 헤지된 포지션을 임의로 건드리지 않는다(사용자 확정 — "유지").

### 시장 상황 [확인 완료] (2026-08-27)

1. 정지 감지 시: 국내 신규·정정 **중단** + 미체결 선주문 **취소**, HL 유지 — 확정.
2. 재개 시: **재개 딜레이(초, 기본 10) 후 발주** — 확정(위).
3. 정지 감지 출처: **LS `JIF` 실시간 웹소켓 구독** — 확정. 세부 상태코드 매핑만 LS `JIF` 문서로 확인.
4. 국내 장기 정지 중 헤지 포지션: **그대로 유지** — 확정.

---

## 9. RT선진입·목표수량 (진입 관점)

- 진입 한 판이 끝날 때마다(후주문 전부 체결) **RT선진입이 계약수만큼 늘어난다.** 목표수량까지 차면 G4에서 이번 계약수가 0이 되어 발주가 멈춘다.
- 청산이 돌아 RT가 줄면 G4가 다시 열려 진입을 이어간다.

---

## 10. 누적 매매결과 (후주문 전부 체결마다) — `DESIGN-auto-m.md §9a`

- `-HP` += HL 매도체결 수량, `+S` += SF 매수체결 수량(계약)
- `-환` = ∑(원달러선물 상대호가 × HL 체결수량) ÷ ∑(HL 체결수량) 로 갱신(평균환율)
- `Sprd` = (누적 HL 매도 평균괴리) − (누적 SF 매수 평균괴리) 로 다시 계산
- 청산은 부호가 반대(`+HP`/`-S`/`+환`).

---

## 결정 로그 (2026-08-27 확정)

1. 주문시간 끝나거나 실행 끄면 미체결 선주문 **취소**.
2. 선주문 일부 체결분은 **즉시** 후주문으로 헤지.
3·8. **후주문은 부분체결·재호가 없음**(즉시 체결형). "후주문 부분체결"은 오타였고, 가격정정은 **선주문 정정**(미체결·부분체결 불문: 남은 선주문 취소→딜레이→다시 걺)뿐. 후주문은 전부 체결 아니면 거부.
4. **선주문 거부 → 딜레이 뒤 다시 냄**(멈추지 않음 — 체결 전이거나 체결분은 이미 헤지).
5. **RT선진입**은 후주문 전부 체결(헤지 완성) 후 **SF 계약수**만큼 증감.
6. 3세트·정/역 모두 **따로 돈다**(서로 간섭 없음).
7. **후주문 거부 → 체결차 → 중지+알림**(다시 안 시도). 헤지 깨진 세트는 멈춤.
9. **체결차는 방향별**로 SF잔고×10 + HL잔고.

## [OPEN] (남은 것)

- 중지 시 **알림 방법**(소리·팝업 등) — 추후.
- **시장 상황(§8)**의 확인 4건(정지 처리·재개 시점·세션 신호 출처·장기 정지 방침).
- **역방향 진입·역방향 청산** — 정/역이 대칭이라 같은 뼈대로 추가(정방향 확정 후, §11 예정).
