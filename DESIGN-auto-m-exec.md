# DESIGN-auto-m-exec.md — 자동M 발주 상태기계 (정방향 진입 상세 + 공통 골격)

> **상태:** 초안(작성 중). **계약의 일부.** `DESIGN-auto-m.md`의 실행(§4~§8)을 상태기계로 상세화한다.
> **왜 중요한가(사용자 강조 2026-08-27):** 이 골격은 자동M만의 것이 아니라 **앞으로 추가될 전략 대부분이 공유하는 공통 흐름**이다. 여기서 상태·이벤트·전이를 제대로 정립해 두면 새 전략은 "상품·부호·판정식·역산식"만 갈아끼우면 된다.
> **범위:** 이 문서는 **정방향 진입** 하나를 끝까지 파고든다. 역방향·청산은 대칭이라 골격을 재사용(추후 각 절 추가).

---

## 0. 재사용 골격 (모든 maker→taker 전략 공통)

**공통 흐름 한 줄:** 판정(게이트 통과) → **선주문(maker)** 발주 → 체결 → **후주문(taker)** 발주 → 양쪽 체결 확인 → 딜레이 → 반복. 예외는 즉시 **중단+알람**.

| 전략마다 **다른** 것 (갈아끼움) | 전략이 **공유**하는 것 (이 문서) |
|---|---|
| 상품(SF/주식/ETF…), 방향·부호 | 상태 집합(State) |
| 판정 조건(어떤 괴리를, 어느 부등호로) | 이벤트 집합(Event) |
| 역산식·기준가·진입범위 공식 | 전이 규칙(누가 무엇으로 어디로) |
| 후주문 수량 배수(SF=×10 등) | 실패·거부·체결차·중단 처리 |

→ 새 전략을 만들 때는 **§2 상태·§3 이벤트·§5 전이표를 그대로 쓰고**, §4 게이트의 판정식과 §6 역산만 그 전략 값으로 바꾼다.

---

## 1. 정방향 진입 — 정의

- **선주문** = LS **주식선물(SF) 매수** (maker, 페깅).
- **후주문** = HL perp **매도** (taker), 수량 = 선주문 체결수량 × 10.
- **성립 방향:** HL 고평가 / LS 저평가일 때(스프레드가 큼).

---

## 2. 상태 (State) — 세트 1개 기준 (3세트는 각자 독립 [OPEN-6])

| 상태 | 뜻 |
|---|---|
| `IDLE` | 진입 실행 OFF, 또는 조건 미충족으로 아무 주문 없음 |
| `ARMED` | 진입 실행 ON. 매 tick 게이트(§4) 평가 |
| `PRE_RESTING` | 선주문 발주됨, 미체결 대기 |
| `PRE_PARTIAL` | 선주문 부분체결 — 체결분 후주문 처리 + 잔량 대기 |
| `POST_PENDING` | 후주문(HL taker) 발주됨, 체결 확인 대기 |
| `SETTLE_DELAY` | 후주문 전량체결 후 선주문 딜레이(ms) 대기 |
| `HALTED` | 중단(선주문 거부·후주문 거부 최종·체결차) + 사운드 알람. 수동 해제 |

---

## 3. 이벤트 (Event)

`tick`(시세 수신) · `조건충족`/`조건미충족` · `선주문_부분체결` · `선주문_전량체결` · `선주문_거부` · `후주문_부분체결` · `후주문_전량체결` · `후주문_거부` · `역산가_변동` · `진입범위_이탈` · `체결차_감지` · `실행_ON`/`실행_OFF` · `시간_종료` · `전환딜레이_경과`

---

## 4. 판정 게이트 (`ARMED`에서 매 tick, 순서대로) — **모두 통과해야** 선주문

| # | 게이트 | 통과 못하면 |
|---|---|---|
| G1 | 진입 실행 ON | `IDLE` |
| G2 | 주문가능시간 2구간 내 | 대기 + 나간 미체결 선주문 **취소**(확정) |
| G3 | 전환딜레이 경과(청산 마지막 체결 후 N초) | 대기 |
| G4 | 진입 가능: `계약수 = Min(1회주문, 목표 − RT선진입)` 이 **> 0** (= RT+계약수 ≤ 목표) | 발주 안 함(목표 도달). 청산으로 RT 줄면 다시 열림(§7) |
| G5 | 판정: (실시간 **SF괴리 > `-HP/+SF`**) **AND** (실시간 **S괴리 > `-HP/+S`**) | 선주문 있으면 **취소** → `ARMED` |
| G6 | 역산가 P(§6) → 호가단위 **내림** → 진입범위 **P ≥ 한계** | 미발주 / 나간 선주문 **취소** |

**모두 통과** → 선주문(LS SF 매수 @P, 계약수) 발주 → `PRE_RESTING`. (이미 선주문이 있으면 유지하되 **P가 바뀌면 정정** — §5 참조.)

- **괴리·역산 수량:** est는 후주문 수량(계약수×10)만큼 HL 매수호가창을 쓸어담은 값(`DESIGN-auto-m.md §6`).

---

## 5. 상태별 전이표

### `PRE_RESTING` (선주문 미체결 대기) — 매 tick
| 이벤트 / 조건 | 처리 → 다음 상태 |
|---|---|
| 게이트 G2·G5·G6 깨짐(시간종료·조건미충족·범위이탈) | 선주문 **취소** → `ARMED`/`IDLE` |
| `역산가_변동` (P 달라짐) | **선주문 정정**: 취소 → 딜레이 → 정정가 재발주 → `PRE_RESTING` |
| `실행_OFF` | 선주문 **취소** → `IDLE` (체결분 포지션은 유지) |
| `선주문_부분체결` | → `PRE_PARTIAL` |
| `선주문_전량체결` | 후주문(HL 매도, 체결수량×10) 발주 → `POST_PENDING` |
| `선주문_거부` | **선주문 딜레이 후 재주문** → `ARMED` (체결 전이라 안전 — 중단 아님) |

### `PRE_PARTIAL` (선주문 부분체결 — 체결분 즉시 헤지, 잔량 대기)
| 이벤트 / 조건 | 처리 → 다음 상태 |
|---|---|
| 부분체결분 발생 | 그 체결분×10 후주문(HL 매도) **즉시 발주**(헤지 지연 최소) → `POST_PENDING` 병행 |
| `역산가_변동` (가격정정) | **선주문 정정**: 잔량 선주문 취소 → 딜레이 → 정정가 재발주. (후주문은 taker라 별도 정정 없음) |
| 잔량 `선주문_전량체결` | (헤지 후주문 발주 완료) → `POST_PENDING` |
| 잔량 `선주문_거부` | **선주문 딜레이 후 재주문** (체결분은 이미 헤지됨) |

### `POST_PENDING` (후주문 HL taker 체결 확인 — 후주문은 부분체결·재호가 없음)
| 이벤트 / 조건 | 처리 → 다음 상태 |
|---|---|
| `후주문_전량체결` | **RT선진입 += 선주문 SF 체결수량**(헤지 완성), 누적 갱신(§6), 체결차 0 확인 → `SETTLE_DELAY` |
| `후주문_거부` | LS 체결분 미헤지 → **체결차 발생 → 중지 + 알림** (재시도 없음) |

### `SETTLE_DELAY`
| 이벤트 | 처리 → 다음 상태 |
|---|---|
| 선주문 딜레이(ms) 경과 | → `ARMED` (다음 선주문 판정) |

### 모든 상태 공통 (인터럽트)
| 이벤트 | 처리 → 다음 상태 |
|---|---|
| `체결차_감지` (**방향별** SF잔고×10 + HL잔고 ≠ 0) | **중지 + 알림**, 재/추가주문 안 함, 수동 처리. 체결 불일치(헤지 깨짐) 세트는 중지 — 후주문 거부가 대표 사례 |
| `실행_OFF` | 나간 미체결 선주문 **취소** → `IDLE` (체결 포지션 유지) |

> **알림 방법은 추후 확정**(사용자 2026-08-27). 지금은 "중지 + 알림"으로만 표기.

---

## 6. 누적 매매결과 갱신 (후주문 전량체결마다) — `DESIGN-auto-m.md §9a`
- `-HP` += HL 매도체결 수량, `+S` += SF 매수체결 수량(계약)
- `-환` = ∑(원달러선물 상대호가 × HL 체결수량) ÷ ∑(HL 체결수량) 갱신
- `Sprd` = (누적 HL 매도 평균괴리) − (누적 SF 매수 평균괴리) 재계산

---

## 7. RT선진입 · 목표수량 (진입 관점)
- 진입 1회 완료(후주문 전량체결)마다 **RT선진입 증가**. 목표 도달(RT=목표)하면 G4에서 계약수 0 → 발주 정지.
- 청산이 돌아 RT가 줄면 G4가 다시 열려 진입 재개(`DESIGN-auto-m.md §7`).

---

## 결정 로그 (2026-08-27 확정)

1. 시간 종료·실행 정지 시 미체결 선주문 **취소**.
2. 선주문 부분체결분은 **즉시** 후주문 발주(헤지 지연 최소).
3·8. **후주문(HL taker)은 부분체결·재호가 개념 없음** — "후주문 부분체결"은 오타였다. 가격정정은 **선주문 정정**(미체결·부분체결 불문: 잔량 선주문 취소→딜레이→재발주)만. 후주문은 전량체결 아니면 거부.
4. **선주문 거부 → 선주문 딜레이 후 재주문**(중단 아님 — 체결 전이거나 부분체결분은 이미 헤지됨).
5. **RT선진입**은 후주문 전량체결(헤지 완성) 후 **선주문 SF 계약수**만큼 증가.
6. 3세트·정/역방향 모두 **독립 상태기계 병행**(상호 게이트 없음).
7. **후주문 거부 → 체결차 발생 → 중지 + 알림**(재시도 없음, LS 체결분 미헤지). 체결 불일치 세트는 중지.
9. **체결차는 방향별**로 SF잔고×10 + HL잔고.

## [OPEN] (남은 것)
- 중지 시 **알림 방법**(사운드/팝업 등) — 추후 확정.
- 역방향 진입 / 정·역 청산의 대칭 전이표 — 정방향 진입 확정 후 같은 골격으로 추가.
