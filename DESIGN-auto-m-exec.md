# 자동M 발주 흐름 — 정방향 진입·청산 상세 (체결쏴)

> **문서 성격:** **자동M(체결쏴)의 단일 스펙(계약)** 이다. 실행 흐름(§0~§10)에 더해, 옛 `DESIGN-auto-m.md`에 있던 전략·화면 스펙(방향·판정·수량·선주문 가격·화면·누적·설정)을 **§11로 통합**했다(2026-09-07 — 문서 둘로 나뉘어 있던 탓에 한쪽만 고쳐 어긋나는 일이 생겨 사용자 결정으로 합침). **체결쏴 개발은 반드시 이 문서를 먼저 읽고 시작한다.**
> **왜 중요한가(사용자 강조 2026-08-27):** 여기서 정한 상태·상황·처리 흐름은 자동M 하나만을 위한 것이 아니다. **앞으로 만들 다른 전략들도 대부분 같은 흐름**(먼저 국내에 걸고, 걸린 게 체결되면 반대편 해외를 즉시 잡는 방식)을 따른다. 그래서 이번에 뼈대를 제대로 세워두면, 새 전략은 "무슨 상품을, 어느 방향으로, 어떤 조건에서" 부분만 바꿔 끼우면 된다.
> **다루는 범위:** **정방향 진입**(§4·§5·§6)과 **정방향 청산**(§7)을 끝까지 파고든다. 역방향(진입·청산)은 좌우가 뒤집힌 대칭이라 같은 뼈대를 재사용하며, 확정 후 이어서 붙인다(§11).

---

## 0. 용어와 공통 뼈대

**두 다리 한 쌍으로 움직인다.** 자동M은 국내 주식선물(SF) 한 자리와 해외 HL 무기한선물 한 자리를 짝지어, 둘의 가격차(괴리)가 벌어질 때 들어가고 좁혀질 때 빠져나온다.

- **선주문** = 국내 SF에 **먼저** 내는 주문. **지정가로 호가에 걸어두고 체결을 기다리는 방식**(maker). 우리가 값을 정한다.
- **후주문** = 선주문이 체결된 뒤 **곧바로** 내는 HL 주문. 상대 1호가에 여유(%)를 얹은 **지정가(Gtc)** 로 내서 taker처럼 바로 잡히게 한다(IOC·FOK 안 씀 — 사용자 확정 2026-09-04). 선주문 체결 수량의 10배(SF 1계약 = HL 10계약). **후주문은 취소하지 않는다**(사용자 확정 2026-09-07) — 일부만 잡히고 잔량이 걸려 있어도 그대로 두고 후주문대기에 머문다. 전부 체결되면 정상 흐름, HL이 거부하면 체결차 → 중지.
- **예상체결가(est)** = 지금 이 수량을 실제로 밀어넣으면 평균 몇 원에 체결될지 호가창을 훑어 계산한 값.
- **역산가** = "이 괴리 기준값을 만족시키려면 선주문을 얼마에 걸어야 하는가"를 거꾸로 푼 값(§5).

**한 판의 흐름(한 줄):** 조건을 통과하면 → 선주문을 호가에 건다 → 체결되면 → 후주문을 즉시 낸다 → 양쪽 다 체결됐는지 확인한다 → 잠깐 쉬었다가(딜레이) → 다시 처음으로. 어딘가 어긋나면(헤지가 깨지면) 즉시 **멈추고 알린다**.

**전략마다 바뀌는 것 / 전략이 공유하는 것:**

| 전략마다 바꿔 끼우는 것 | 모든 전략이 함께 쓰는 뼈대(이 문서) |
|---|---|
| 상품(주식선물·주식·ETF …), 사고팔 방향 | 상태 목록(§2) |
| 판정 조건(어떤 괴리를 어느 부등호로 볼지) | 일어나는 일(신호) 목록(§3) |
| 역산 공식·기준가·발주 허용범위 | 상태가 어떻게 넘어가는지(§4~§7) |
| 후주문 수량 배수(SF는 10배) | 실패·거부·헤지 어긋남·멈춤 처리 |

---

## 1. 정방향 진입이란

**정방향**은 "HL이 비싸고 국내가 싼" 국면을 노린다. 이때 **싼 국내를 사고(선주문 SF 매수) 비싼 HL을 판다(후주문 HL 매도).** 나중에 둘의 차이가 좁혀지면 청산(§7)에서 반대로 되돌려 차익을 남긴다.

- **선주문:** 국내 SF **매수**를, 우리가 정한 값에 호가로 건다.
- **후주문:** 선주문이 체결된 만큼 HL을 **매도**로 즉시 잡는다(체결 수량 × 10계약).
- **들어가는 조건:** 국내현물(S) 괴리가 기준(`-HP/+S`)을 넘어야 한다(§4 G5, 정정 2026-09-07 — 선물 괴리는 비교하지 않고 그 기준값을 역산가에 쓴다).

---

## 2. 상태 목록 (세트 한 개 기준)

한 세트는 아래 일곱 상태 중 하나에 있다. 3세트·정역은 각각 따로 돈다(§4 G-동시성).

| 상태 이름 | 코드값 | 무슨 상황인가 |
|---|---|---|
| **대기** | `IDLE` | 진입 실행을 껐거나, 조건이 안 맞아 아무 주문도 없는 상태 |
| **감시** | `ARMED` | 진입 실행을 켠 상태. 시세가 들어올 때마다 §4 조건을 하나하나 따진다 |
| **선주문대기** | `PRE_RESTING` | 선주문(국내 SF)을 걸어두고 체결을 기다리는 중 |
| **부분체결** | `PRE_PARTIAL` | 선주문이 일부만 체결됨. 체결된 만큼은 후주문으로 헤지하고, 남은 수량은 계속 대기 |
| **후주문대기** | `POST_PENDING` | 후주문(HL)을 즉시로 내고 체결 확인을 기다리는 중 |
| **딜레이대기** | `SETTLE_DELAY` | 한 판이 끝나 다음 선주문까지 잠깐 쉬는 중(선주문 딜레이) |
| **중지** | `HALTED` | 헤지가 깨져 멈춘 상태(후주문 거부로 인한 체결차, 또는 체결차 감지) + 소리 알림. **사람이 직접 풀어야 재개.** 중지되면 실행은 꺼진다(버튼 원색, 행은 검정). 화면에서 그 다리의 실행 버튼을 누르면 "중지 해제" 확인창(사유 표시) → 예: 코어 `autom_release` 뒤 실행, 아니오: 그대로(2026-09-07). 선주문 거부는 여기 해당 없음(잠깐 쉬었다 다시 냄) |

---

## 3. 일어나는 일(신호) 목록

상태를 바꾸는 계기들이다.

시세 들어옴 · 조건 충족/미충족 · 선주문 일부체결 · 선주문 전부체결 · 선주문 거부 · 후주문 전부체결 · 후주문 거부 · 역산가 바뀜 · 발주범위 벗어남 · 체결차 생김 · 실행 켬/끔 · 주문시간 끝남 · 전환대기 끝남 · **시장 정지/재개**(§8)

---

## 4. 발주 통과 조건 (감시 상태에서 시세마다, 순서대로)

진입 실행을 켜면 세트는 **감시** 상태로 들어가, 시세가 올 때마다 아래 여섯 조건을 **차례로** 따진다. **하나라도 걸리면 선주문을 내지 않고, 이미 걸어둔 게 있으면 대개 취소**한다. 여섯을 다 통과해야 선주문을 건다.

| 순서 | 조건 | 통과 못하면 |
|---|---|---|
| G1 | 진입 실행이 켜져 있는가 | 대기 상태로 |
| G2 | 지금이 주문 가능한 시간대(2구간) 안인가 | 안 냄 + 걸어둔 미체결 선주문 **취소** |
| G3 | 전환대기가 끝났는가(직전 청산 체결 뒤 N초 지났는가) | 안 냄 |
| G4 | 더 들어갈 여유가 있는가 — 이번에 낼 계약수 = `Min(1회주문수량, 목표수량 − RT선진입)` 이 **1 이상**인가 | 안 냄(목표를 다 채운 상태). 청산으로 RT가 줄면 다시 열림(§9) |
| G5 | **판정:** 국내현물 괴리 > `-HP/+S` (정정 2026-09-07 — 선물 괴리는 비교하지 않음. `-HP/+SF`는 G6 역산가에만 쓴다) | 걸어둔 선주문 있으면 **취소** |
| G6 | **역산가가 발주 허용범위 안인가** — 역산가 계산 → 호가단위로 내림 → 허용 하한(한계) 이상인가 | 안 냄 / 걸어둔 선주문 **취소** |

**여섯 다 통과 →** 선주문(국내 SF 매수, 계약수만큼, 역산가에) 발주 → **선주문대기**로. 이미 같은 선주문이 걸려 있으면 그대로 두되, **역산가가 바뀌었으면 취소 → 딜레이 → 신규주문**한다(§6 "선주문 재발주 공통 규칙").

### G5·G6을 풀어 쓰면

1. **먼저 G5(판정).**
   - 현물(S) 괴리가 **기준 미달**이면 → 걸어둔 선주문이 있으면 취소하고, 새로 내지 않는다.
   - 넘으면 → G6으로 간다. (선물 괴리는 여기서 비교하지 않는다 — 정정 2026-09-07. 선주문 자체가 `-HP/+SF`가 체결로 보장되는 가격에 걸리므로 따로 볼 이유가 없다.)
2. **다음 G6(역산 → 허용범위).**
   - 역산가를 구하고(§5), 국내 SF 호가단위로 **내림**한다(매수라 유리하게).
   - 그 값이 **허용 하한(한계)보다 낮으면**(= 너무 싸게 사려고 물러나 있어 체결 가망이 낮으면) → 안 내고, 걸어둔 게 있으면 취소.
   - 허용 하한 이상이면 → 발주(또는 값이 달라졌으면 정정).
   - 이 계산은 **시세가 올 때마다 다시** 한다. 그래서 걸어둔 선주문도 시세가 움직이면 역산가를 새로 구해 재점검하고, 값이 바뀌면 정정한다.

> **실예 (숫자로).** 하이닉스 선물이론가 200,000원, 진입SF 기준값 0.5%, HL 예상체결 괴리 1.0%, 선주문 호가단위(틱) 3,000원, 발주범위 0.4%, 국내 SF 매도1호가 201,500원이라 하자.
> - **역산가** = 200,000 × (1 + 1.0% − 0.5%) = 200,000 × 1.005 = **201,000원** (틱 3,000의 배수라 내림해도 201,000).
> - **허용 하한(한계)** = (매도1호가 − 1틱) × (1 − 범위) = (201,500 − 3,000) × (1 − 0.4%) = 198,500 × 0.996 = **약 197,706원**.
> - 역산가 201,000 ≥ 한계 197,706 → **선주문을 201,000원에 매수로 건다.** (매도1호가 201,500보다 낮으니 바로 체결되지 않고 호가에 걸린다.)
> - 만약 시세가 밀려 역산가가 197,000원까지 내려가면(한계 미달) → 그 선주문은 **취소**한다.

---

## 5. 역산가와 발주 허용범위 (G6 상세)

- **역산가** `P = 선물이론가 × (1 + HL예상체결괴리 − 기준값)`. HL예상체결괴리는 후주문 수량(계약수×10)만큼 HL 매수호가창을 훑은 값(자세히는 `§11.6`).
- **호가단위 내림:** 매수는 싸게 살수록 유리하니, 국내 SF 선주문 호가단위(하이닉스 3,000·삼성 500·현대차 1,000)로 **내림**한다.
- **발주 허용범위(한계):** 매수는 `한계 = (상대 매도N호가 − 1틱) × (1 − 범위)`. 역산가가 이 값 **이상**이어야 낸다. 여기서 **상대 매도N호가는 그 시점 호가창의 실제 N번째 매도호가**(공통설정 콤보에서 1~5 선택). 빈 호가는 건너뛴다.

---

## 6. 상태가 넘어가는 규칙 (정방향 진입)

각 상태에서 어떤 일이 생기면 어떻게 처리하고 어디로 가는지다. **§4를 통과해 선주문을 건 뒤부터**를 다룬다.

### 선주문 재발주 공통 규칙 (확정 2026-09-03 — 정/역방향 진입·청산 모두)

역산가가 바뀌어 선주문을 다시 낼 때 "정정"은 없다. **항상 새 주문을 내는 것처럼** 한다.

1. **미체결 수량 취소.** 이미 체결된 부분(과 그 헤지)은 손대지 않는다.
2. **병행 중인 후주문이 있으면 그 체결을 먼저 확인한다.** 부분체결분의 HL 헤지가 체결돼 RT선진입이 갱신된 뒤에야 다음으로 간다(확인 전에 내면 RT가 옛 값이라 목표 근처에서 남은 양보다 많이 낼 수 있다). 후주문이 거부되면 기존 규칙대로 체결차 → 중지+알림이고 신규주문은 내지 않는다.
3. **딜레이**(선주문 딜레이).
4. **신규주문.** 수량은 처음 낼 때와 같은 식으로 **그 시점에 다시 계산**한다 — 진입 `Min(1회주문수량, 목표수량 − RT선진입)`, 청산 `Min(1회주문수량, RT선진입)`. "남은 수량"을 그대로 쓰지 않는다. 가격은 새 역산가(진입 내림 / 청산 올림).

이 규칙은 상태(선주문대기·부분체결)와 방향(정/역)·블록(진입/청산)에 관계없이 같다.

### 선주문대기 (`PRE_RESTING`) — 시세마다 재점검

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| G2·G5·G6 중 하나 깨짐(시간 끝·조건 미달·범위 벗어남) | 선주문 **취소** | 감시/대기 |
| 역산가 바뀜 | 미체결 수량 **취소** → 딜레이 → **신규주문**(계약수 = Min(1회주문수량, 목표수량 − RT선진입), 새 역산가) | 선주문대기 |
| 실행을 끔 | 선주문 **취소**(이미 체결된 포지션은 그대로 둠) | 대기 |
| 선주문 일부 체결 | — | 부분체결 |
| 선주문 전부 체결 | 후주문(HL 매도, 체결수량×10) 즉시 발주 | 후주문대기 |
| 선주문 거부 | **딜레이 뒤 다시 냄**(아직 체결 전이라 위험 없음 — 멈추지 않음) | 감시 |

### 부분체결 (`PRE_PARTIAL`) — 체결된 만큼 즉시 헤지, 남은 건 계속 대기

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 일부 체결됨 | 체결된 수량 × 10을 HL 매도로 **즉시** 냄(헤지 지연 최소) | 후주문대기 병행 |
| 역산가 바뀜 | **미체결 수량 취소** → **병행 후주문 체결 확인(RT 갱신)** → 딜레이 → **신규주문**(계약수 = Min(1회주문수량, 목표수량 − RT선진입), 새 역산가 — 남은 수량이 아니라 처음 낼 때와 같은 식, 사용자 확정 2026-09-03). 이미 체결·헤지된 부분은 그대로 | 선주문대기 |
| 남은 수량 전부 체결 | 그 몫 후주문 발주 | 후주문대기 |
| 남은 수량 거부 | 딜레이 뒤 다시 냄(체결된 부분은 이미 헤지됨) | 감시 |

> **실예.** 선주문 10계약을 걸었는데 4계약만 체결됐다. → 즉시 HL 40계약을 매도로 낸다(4×10). 남은 6계약은 그대로 호가에 걸려 있다. 이때 시세가 움직여 역산가가 바뀌면, **남은 6계약을 취소**하고, 4계약 헤지(HL 40계약) **체결을 확인해 RT가 4 늘어난 뒤**, 딜레이를 두고 **신규주문**을 낸다 — 수량은 6이 아니라 `Min(1회주문 10, 목표 100 − RT 4) = 10`계약을 새 역산가에 건다. 이미 잡은 4계약(SF)+40계약(HL)은 손대지 않는다.

### 후주문대기 (`POST_PENDING`) — 지정가(Gtc) taker, 재호가 없음

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 후주문 전부 체결 | **RT선진입 += 선주문 SF 체결계약수**(헤지 완성), 누적 결과 갱신(§10), 체결차 0 확인 | 딜레이대기 |
| 후주문 거부 | 국내는 체결됐는데 HL이 안 잡힘 → **체결차 발생 → 중지 + 알림**(다시 안 시도) | 중지 |
| 후주문 일부만 체결, 잔량이 걸려 있음 | **취소하지 않는다**(사용자 확정 2026-09-07). HL 대기 = 잔량(소수 그대로)으로 후주문대기 유지, 그동안 새 선주문 없음. 잔량이 다 잡히면 ㄹ1 | 후주문대기 |
| 후주문이 밖에서 끝남(사람이 취소 · HL 거부 · **HL이 증거금 한도로 잔량을 버리고 'filled'로 종료**[실측 2026-09-07, 문서의 marginCanceled와 다름] — 엔진은 취소 안 함) | 미체결분만큼 체결차 → **중지 + 알림** | 중지 |

### 딜레이대기 (`SETTLE_DELAY`)

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 선주문 딜레이(ms) 지남 | 다음 판정으로 | 감시 |

### 어느 상태에서든 끼어드는 일

| 일어난 일 | 처리 | 다음 |
|---|---|---|
| 체결차 생김(방향별 SF잔고×10 + HL잔고 ≠ 0) | **중지 + 알림**, 추가·재주문 안 함, 사람이 직접 정리. 헤지가 깨진 세트는 멈춤(후주문 거부가 대표) | 중지 |
| 실행을 끔 | 걸어둔 미체결 선주문 **취소**(체결 포지션은 유지) | 대기 |

> 알림 방법(소리·팝업 등)은 **추후 확정**(사용자 2026-08-27). 지금은 "중지 + 알림"으로만 적는다.

---

## 6A. 경우의 수 — 정방향 진입 (상황별로 전부 나열)

위 규칙을 "이런 상황이면 이렇게"로 다시 펼친 것이다. 진입에서 나올 수 있는 경우를 단계별로 모두 적는다.

### ㄱ. 선주문을 낼지 판정할 때 (감시)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄱ1 | 조건 통과 + 역산가가 허용범위 안 | 선주문(SF 매수, 계약수=Min(1회주문, 목표−RT))을 역산가에 걺 | 선주문대기 |
| ㄱ2 | 두 괴리 중 하나라도 미달 | 안 냄(걸어둔 선주문 있으면 취소) | 대기 |
| ㄱ3 | 조건은 맞는데 역산가가 허용 하한보다 낮음(너무 물러남) | 안 냄(있으면 취소) | 대기 |
| ㄱ4 | 지금이 주문 가능 시간대가 아님 | 안 냄 + 미체결 선주문 취소 | 대기 |
| ㄱ5 | 전환대기가 안 끝남(직전 청산 체결 뒤 N초 이내) | 안 냄 | 대기 |
| ㄱ6 | 목표를 다 채움(RT=목표라 이번 계약수가 0) | 안 냄 | 대기(청산으로 RT 줄면 다시 열림) |
| ㄱ7 | 목표까지 남은 양이 1회주문보다 적음(목표−RT < 1회주문) | 계약수를 남은 양으로 **줄여** 발주 | 선주문대기 |

> **실예 (ㄱ7).** 목표 100계약, 이미 95계약 진입(RT=95), 1회주문 10계약. → 이번엔 10이 아니라 `Min(10, 100−95)=5`계약만 낸다.

### ㄴ. 선주문을 걸어두고 기다릴 때 (선주문대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄴ1 | 역산가가 바뀜 | 취소 → 딜레이 → 신규주문(계약수 = Min(1회주문, 목표−RT), 새 역산가) | 선주문대기 |
| ㄴ2 | 조건이 미달로 바뀜 | 선주문 취소 | 감시 |
| ㄴ3 | 역산가가 허용범위를 벗어남 | 선주문 취소 | 감시 |
| ㄴ4 | 주문 가능 시간대가 끝남 | 선주문 취소 | 대기 |
| ㄴ5 | 선주문이 거부됨 | 딜레이 뒤 다시 냄(멈추지 않음) | 감시 |
| ㄴ6 | 선주문이 일부만 체결됨 | 체결분×10 후주문 즉시 발주 | 부분체결 |
| ㄴ7 | 선주문이 전부 체결됨 | 후주문(전량×10) 발주 | 후주문대기 |

### ㄷ. 선주문이 일부만 체결됐을 때 (부분체결)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄷ1 | 역산가가 바뀜 | 미체결 수량 취소 → 병행 후주문 체결 확인(RT 갱신) → 딜레이 → 신규주문(계약수 = Min(1회주문, 목표−RT), 새 역산가). 헤지된 부분은 그대로 | 선주문대기 |
| ㄷ2 | 남은 수량이 전부 체결됨 | 그 몫 후주문 발주 | 후주문대기 |
| ㄷ3 | 남은 수량이 거부됨 | 딜레이 뒤 다시 냄(체결분은 이미 헤지) | 감시 |

### ㄹ. 후주문을 내고 확인할 때 (후주문대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄹ1 | 후주문 전부 체결 | RT += SF 체결계약수, 누적 갱신, 체결차 0 확인 | 딜레이대기 |
| ㄹ2 | 후주문 거부 | 체결차 발생 → **중지 + 알림**(다시 안 시도) | 중지 |

> **실예 (ㄹ1).** 선주문 10계약(SF 매수)이 다 체결되고 후주문 HL 100계약(매도)도 다 체결됐다. → RT선진입이 10계약 늘고, 누적 매도수량·평균환율·실현 스프레드를 갱신한 뒤, 선주문 딜레이만큼 쉬고 다음 판정으로 돌아간다.

### ㅁ. 쉬었다가 반복 (딜레이대기)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㅁ1 | 후주문 전부 체결 후 | 선주문 딜레이(ms)만큼 대기 → 다음 판정 | 감시 |

### ㅂ. 예외·실행 끄기 (어느 상태에서든)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㅂ1 | 체결차 생김(방향별 SF잔고×10 + HL잔고 ≠ 0) | 중지 + 알림, 사람이 정리 | 중지 |
| ㅂ2 | 실행을 끔 | 미체결 선주문 취소, 체결 포지션은 유지 | 대기 |

### ㅅ. 여러 개가 동시에 (동시성)

| # | 상황 | 처리 |
|---|---|---|
| ㅅ1 | 3세트가 동시에 조건 통과 | 세트마다 **따로** 위 흐름을 밟는다(서로 간섭 없음) |
| ㅅ2 | 정방향·역방향이 동시에 성립 | 방향마다 **따로** 돈다(서로 다른 상품이라 간섭 없음) |

---

## 7. 경우의 수 — 정방향 청산 (상황별로 전부 나열)

**청산은 진입을 되돌리는 것.** 정방향 청산은 국내 SF **매도**(들고 있던 롱을 판다)를 먼저 걸고, 체결되면 HL을 **매수**(들고 있던 숏을 되산다)로 즉시 잡는다. 상태·신호·흐름은 진입과 똑같고, **아래만 좌우가 뒤집힌다.**

### 진입과 다른 점 (한눈에)

| 항목 | 정방향 진입 | 정방향 청산 |
|---|---|---|
| 전환대기(G3) | 직전 **청산** 체결 뒤 N초 | 직전 **진입** 체결 뒤 N초 |
| 이번 계약수(G4) | Min(1회주문, **목표−RT**) | Min(1회주문, **RT선진입**) — 즉 RT가 0보다 커야 함 |
| 판정(G5) | 현물괴리 > 기준(`-HP/+S`) — 선물괴리는 비교 안 함(정정 2026-09-07) | **조건 비교 없음** — `+HP/-SF`는 역산가에만(정정 2026-09-07) |
| 역산·범위(G6) | HL **매수**호가창, **내림**, 역산가 **≥ 한계**((상대매도−1틱)(1−범위)) | HL **매도**호가창, **올림**, 역산가 **≤ 한계**((상대매수+1틱)(1+범위)) |
| RT선진입 | 후주문 체결 시 **늘어남** | 후주문 체결 시 **줄어듦** |
| 누적 표기 | `-HP`/`+S`/`-환` | `+HP`/`-S`/`+환` |

> **왜 반대인가.** 진입 때 국내를 샀으니(롱) 청산은 판다. 살 때는 싸게 살수록 좋아 호가단위를 내리고 "역산가가 하한 이상"이면 걸지만, 팔 때는 비싸게 팔수록 좋아 호가단위를 **올리고** "역산가가 상한 이하"면 건다.

### ㄱ. 선주문을 낼지 판정할 때 (감시)

| # | 상황 | 처리 | 결과 |
|---|---|---|---|
| ㄱ1 | 선물괴리 < 기준 + 역산가가 청산 허용범위 안(≤ 한계) | 선주문(SF 매도, 계약수=Min(1회주문, RT))을 역산가에 걺 | 선주문대기 |
| ㄱ2 | 선물괴리 ≥ 기준(미달) | 안 냄(걸어둔 선주문 있으면 취소) | 대기 |
| ㄱ3 | 조건은 맞는데 역산가가 상한보다 높음(너무 물러남) | 안 냄(있으면 취소) | 대기 |
| ㄱ4 | 주문 가능 시간대가 아님 | 안 냄 + 미체결 취소 | 대기 |
| ㄱ5 | 전환대기가 안 끝남(직전 진입 체결 뒤 N초 이내) | 안 냄 | 대기 |
| ㄱ6 | 청산할 물량이 없음(RT=0) | 안 냄 | 대기(진입으로 RT 늘면 다시 열림) |
| ㄱ7 | RT가 1회주문보다 적음 | 계약수를 RT로 **줄여** 발주 | 선주문대기 |

> **실예 (ㄱ7).** RT선진입 3계약, 1회주문 10계약. → 이번엔 `Min(10, 3)=3`계약만 청산으로 낸다(들고 있는 만큼만 되돌린다).

### ㄴ~ㅅ. 나머지

진입(§6A ㄴ~ㅅ)과 상태·처리가 같고 방향만 반대다.

- **ㄴ(선주문대기)·ㄷ(부분체결):** 선주문 = SF 매도, 후주문 = HL 매수. 역산가 바뀌면 §6 공통 규칙대로 미체결 취소 → (병행 후주문 체결 확인) → 딜레이 → 신규주문(계약수 = **Min(1회주문, RT선진입)**을 그 시점에 다시 계산, 새 역산가 올림). 조건 미달·범위 이탈·시간 끝나면 취소, 거부되면 딜레이 뒤 다시 냄, 일부 체결되면 그만큼 HL 매수로 즉시 헤지.
- **ㄹ(후주문대기):** 후주문(HL 매수) 전부 체결 → **RT선진입 −= SF 체결계약수(줄어듦)**, 누적(`+HP`/`-S`/`+환`) 갱신, 체결차 0 확인 → 딜레이대기. 후주문 거부 → 체결차 → 중지+알림.
- **ㅁ(딜레이)·ㅂ(예외·끄기)·ㅅ(동시성):** 진입과 완전히 동일.

---

## 8. 시장 상황 — 정지와 재개 (사이드카·서킷브레이커·VI·거래정지)

지금까지는 "정상적으로 접속매매가 도는" 시장을 전제했다. 실제로는 국내 시장이 여러 이유로 **잠깐 멈췄다 풀리는** 일이 있다. 이 구간을 다루지 않으면, 멈춘 사이 엉뚱한 주문을 내거나 헤지가 어긋날 수 있다.

> **핵심 위험(꼭 짚을 것):** 국내(SF·현물)가 멈춰도 **HL은 24시간 계속 거래된다.** 그래서 국내만 멈춘 동안에는
> - 새 진입/청산을 **낼 수 없고**(국내 다리를 못 잡으니 한쪽만 잡히면 헤지가 깨진다),
> - 이미 잡아둔 포지션은 **HL 쪽만 값이 계속 움직인다**(체결 불일치는 아니지만 평가손익이 흔들린다).
> 따라서 국내 정지 신호가 오면 **국내 신규·정정 발주를 즉시 멈추는 것**이 안전의 기본이다.

**감지 방법(확정 2026-08-27):** 시간을 하드코딩하지 않고, **LS `JIF`(장운영정보) 실시간 웹소켓 구독**으로 장운영 상태(거래 정상·정지·단일가·VI·사이드카·서킷브레이커 등)를 실시간으로 받아 **세션(§DESIGN 세션 주도)**에 반영한다. 세션이 "거래 불가"를 알리면 데드존(신규 발주 금지)으로 취급한다. — 구체 필드·상태코드 매핑은 **LS `JIF` 문서 기준으로 확정**(추측 금지). (HL은 24시간 거래라 이런 정지 개념이 없어, 이 감지는 국내 다리 전용.)

### 정지 처리 모델 (확정 2026-08-27)

JIF `jstatus`는 성격이 셋으로 갈리고, **`jangubun`(시장)에 따라 같은 번호도 뜻이 다르다**(예: 61이 주식은 서킷1발동, 선물은 당일 장종료).

- **시간대 코드** → `SessionPhase`(장전동시호가·장시작·장마감·프리·애프터 등).
- **정지 발동/해제 코드** → **시장별 "정지 사유 오버레이"**(phase와 별개로 얹는다). 발동이면 사유 설정, 해제면 해제.
- **변동성 확대 코드(선물 70~77)** → **거래 계속**(정지 아님, 정보로만).

**핵심 규칙:**
- **발주 가능 = 시간대(phase)가 거래시간 AND 그 시장에 정지 오버레이 없음.** 정지 오버레이가 있으면 데드존과 같게(신규 발주 금지 + 미체결 취소).
- **시장별로 따로 본다.** 자동M은 선물(`jangubun 5`) 오버레이만 본다 → **주식 사이드카(`jangubun 1`, 64/66)는 자동M 발주를 막지 않는다**(결정 3). 자동T(주식)는 주식 오버레이를 본다.
- 발동/해제는 **짝**이다 — 해제 코드를 반드시 매핑해야 정지에서 풀린다.

**jstatus 분류(주요):**
- 정지 발동(주식 1,2): 61·68 서킷1·2발동, 69 서킷3발동(+당일종료), 63 서킷1 동시호가종료, 64·66 사이드카 매도·매수발동.
- 정지 해제(주식): 62·70 서킷 해제(호가접수개시), 65·67 사이드카 해제.
- 정지 발동/해제(선물 5): 63 서킷(장중동시마감)=발동, 62 서킷 해제, 61 당일 장종료(=DEAD).
- 변동성(선물 70~77): 거래 계속(정보).
- (세부 코드값은 LS `JIF` 코드표 그대로 — 구현 시 매핑 표로.)

### 상황별 처리

| 상황 | 무엇인가 | 국내 다리(SF) | 자동M 처리(제안) |
|---|---|---|---|
| **사이드카** | 선물이 급변(±5~6%)해 **프로그램매매 호가효력이 5분 정지** | 프로그램매매로 분류되면 신규·정정 5분 정지 | 국내 신규·정정 **중단**. 걸어둔 미체결 선주문 취소. HL은 손대지 않음. 5분 뒤 자동 재개 |
| **서킷브레이커(매매중단)** | 지수 급락 단계별 — 1단계 −8%(20분 정지+단일가 재개), 2단계 −15%, 3단계 −20%(당일 종료) | 전 종목 매매 정지 | 국내 신규·정정·취소 모두 **중단**(정지 중엔 취소도 불가할 수 있음). 재개 시 정상 판정 복귀 |
| **VI(변동성완화장치)** | 개별종목 급변 시 **2~10분 단일가매매**로 전환(접속매매 정지) | 접속매매 정지, 단일가만 | 그 종목 신규·정정 **중단**(호가에 걸어두는 방식이 단일가에선 무의미). 해제되면 복귀 |
| **개별 거래정지** | 공시·이상급등 등으로 그 종목 거래 정지 | 거래 정지 | 그 종목 세트 **중단**, 걸어둔 선주문 취소. 재개까지 대기 |
| **장 시작/마감 동시호가** | 접속매매가 아닌 단일가 시간대 | 단일가 | 주문가능시간(§4 G2)에서 이미 제외(2구간이 접속매매 시간이라야 함) |

### 재개(풀림) 처리 (확정 2026-08-27)

- 정지가 풀려 **접속매매로 돌아오면**, 세트는 다시 **감시**부터 정상 판정을 밟는다.
- **재개 딜레이:** 재개 직후는 단일가→접속매매 전환으로 변동이 크므로, **바로 발주하지 않고 "재개 딜레이(초)"만큼 쉰 뒤** 판정을 시작한다. **공통설정창에 "재개 딜레이(초)" 항목 신설, 기본값 10초.**
- **정지 중 포지션은 그대로 유지.** 국내가 오래(서킷 20분+) 멈춰 HL만 값이 움직여도, 이미 헤지된 포지션을 임의로 건드리지 않는다(사용자 확정 — "유지").

### 시장 상황 [확인 완료] (2026-08-27)

1. 정지 감지 시: 국내 신규·정정 **중단** + 미체결 선주문 **취소**, HL 유지 — 확정.
2. 재개 시: **재개 딜레이(초, 기본 10) 후 발주** — 확정(위).
3. 정지 감지 출처: **LS `JIF` 실시간 웹소켓 구독** — 확정. 세부 상태코드 매핑만 LS `JIF` 문서로 확인.
4. 국내 장기 정지 중 헤지 포지션: **그대로 유지** — 확정.

---

## 9. RT선진입·목표수량 (진입 관점)

- 진입 한 판이 끝날 때마다(후주문 전부 체결) **RT선진입이 계약수만큼 늘어난다.** 목표수량까지 차면 G4에서 이번 계약수가 0이 되어 발주가 멈춘다.
- 청산이 돌아 RT가 줄면 G4가 다시 열려 진입을 이어간다.

---

## 10. 누적 매매결과 (체결마다 갱신 — 자동T §7a를 SF로, **사용자 확정 2026-09-04**)

매매결과 라벨은 목업 그대로 `-HP`/`+S`/`-환`이나, 자동M의 국내 체결은 **SF**다. **정방향 진입** 예:

- **`-HP`** = 누적 **HL 매도체결 수량**(HL은 소수 계약 가능 — 실측 2026-09-07)
- **`+S`** = 누적 **SF 매수체결 수량**(계약)
- **`-환`(환진입가)** = HL 체결 시점의 **원달러선물 매수1호가**를 HL 체결수량으로 가중평균한 값. 정방향 진입은 −환(원달러선물 매도 방향)이라 팔리는 값인 매수호가를 쓴다(사용자 확정). 반대 방향(+환)은 매도1호가. 호가가 없으면 직전 체결가. (엑셀 메인 J1/J2 = 시세!H65 매도호가/F65 매수호가는 **평가·청산용** 기준이고, 진입 환은 I27/I35 입력값.)
- **`Sprd`(진입)** = `(환진입가 × HL매도 평균진입가 − S현재가) / S현재가 − (SF매수 평균진입가 − SF이론가) / SF이론가`
  — 엑셀 메인 **I25** `=(I27*I28-$K$7)/$K$7-(I29-$H$20)/$H$20` (I27 환, I28 HL 진입가, I29 SF 진입가, K7=시세!AA60 **주식 현재가(실시간)**, H20=시세!AG61 **SF이론가(실시간)**). 즉 진입 평균가는 고정, 기준(주식 현재가·SF 이론가)은 실시간이라 값이 시세 따라 움직인다. HL 평균진입가·SF 평균진입가는 누적 체결수량 가중평균.
- **`Sprd`(청산)** = `(환진입가 × HL매수 평균청산가 − S현재가) / S현재가 − (SF매도 평균청산가 − SF이론가) / SF이론가` (엑셀 메인 I33 패턴).
- **역방향**은 부호 반대(`+HP`/`-S`/`+환`, 환진입가 = 원달러선물 매도1호가).
- `clear` 버튼 = 위 누적값 0으로 초기화(세트설정 "체결차 Clear"는 체결차만 — 별개).
- **중지 알림(확정 2026-09-04):** 체결차 발생·후주문 거부로 세트가 멈추면 ① 공통설정 "에러 발생 시" 알람 소리 재생, ② 그 세트 행을 **검은 배경·흰 글자**로, ③ 메인창 상태줄에 표시. 팝업은 띄우지 않는다(모달이 다른 조작을 막음).
- **구현 순서(확정 2026-09-04):** 정방향(진입·청산) 먼저 완성·실측 → 역방향은 대칭으로 뒤에.
- [OPEN] `Sprd` 실현식(HL 평균괴리 − SF 평균괴리)이 의도와 맞는지 사용자 검토 대기.

---

## 11. 전략·화면 스펙 (옛 `DESIGN-auto-m.md` 통합, 2026-09-07)

> **원본:** 화면 목업 `docs/STG_2/`(`layout_1.png`·`체결쏴 설정.png`·`세트설정.png`, 2026-08-26) + 사용자 문답(2026-08-26, `체결쏴(자동M).txt`). 실행 흐름(상태·이벤트·실패 처리)은 §0~§8, 누적 계산은 §10 — 여기서는 되풀이하지 않는다.

### 11.1 개요
- 자동M = **HL perp ↔ 국내 주식선물(SF)** 한 쌍. **선주문(LS SF maker) 체결 → 후주문(HL taker)**. 동시 taker인 자동T(HL–주식)와 별개 화면.
- 주로 **장중** 사용.
- 한 종목에 **정방향·역방향 동시 진행** 가능(정방향=SF 롱 / 역방향=SF 숏). **SF는 선물이라 숏에 업틱룰이 없다**(자동T 대주와 다른 점).
- **SF 1계약 = 주식 10주 = HL 10계약.** 1회주문 단위 = **계약**. HL 체결은 소수 계약으로 올 수 있다(0.588 등, 실측 2026-09-07) — 체결차·HL 대기는 소수 그대로 다룬다.

### 11.2 방향 정의
| 방향 | 시장 상태 | 진입 액션 | 청산 액션 |
|---|---|---|---|
| **정방향** | HL 고평가 / LS 저평가 | `-HP/+SF` : HL 매도 + SF 매수 | `+HP/-SF` : HL 매수 + SF 매도 |
| **역방향** | LS 고평가 / HL 저평가 | `+HP/-SF` : HL 매수 + SF 매도 | `-HP/+SF` : HL 매도 + SF 매수 |

- SF 매도 = 선물 숏(공매도 아님). `DESIGN §3` 공매도 불변식과 무관.

### 11.3 판정
**진입은 S(현물) 괴리만 비교하고, 청산은 조건을 비교하지 않는다**(정정 2026-09-07 — 옛 "SF·S 둘 다 AND / 청산 SF" 폐기). SF 기준값(`-HP/+SF`·`+HP/-SF`)은 판정이 아니라 **선주문 역산가(§11.6)** 에 쓴다 — 선주문이 그 기준값이 체결로 보장되는 가격(maker)에 걸리므로 SF 괴리를 따로 비교할 이유가 없다. 각 세트는 계약수로 est를 구해 실시간 괴리를 계산한다.

| 신호 | 발주 조건 | 액션 |
|---|---|---|
| 정방향 진입 | 실시간 S괴리 > `-HP/+S` | HL 매도 + SF 매수 |
| 정방향 청산 | 조건 비교하지 않음 (G2~G4·G6만) | HL 매수 + SF 매도 |
| 역방향 진입 | 실시간 S괴리 < `+HP/-S` | HL 매수 + SF 매도 |
| 역방향 청산 | 조건 비교하지 않음 | HL 매도 + SF 매수 |

- **S괴리** = HL_est괴리 − 현물괴리. 부등호 방향은 자동T와 동일(정방향 진입 `>` / 역방향 `<`).
- **S(현물) 괴리는 진입 허용 조건으로만** 쓰고, 실제 매매는 SF+HL. 청산은 S를 안 본다. S 괴리의 est 수량 = **계약수 × 10 주** 등가(확정 2026-08-26).
- 편집 가능 컬럼은 방향당 3개: 진입 `SF`·진입 `S`·청산 `SF`.

### 11.4 실행 요약 (상세는 §4~§6·§ㄴ~§ㄹ)
- **주문순서:** 선주문(LS SF maker) 체결 → 후주문(HL taker) 발주. **후주문 수량 = 선주문 체결수량 × 10.**
- **모든 주문은 지정가**(사용자 확정 2026-09-04 — IOC·FOK 안 씀). 후주문 가격: HL 매도 = 매수1호가 × (1 − 후주문 HP 매도) / HL 매수 = 매도1호가 × (1 + 후주문 HP 매수). HP(%)는 체결쏴 설정(자동T HP와 같은 뜻, 기본 1%). 그 가격은 **HL 가격 격자(유효숫자 5자리·소수 6−szDecimals, HL 공식 문서 'Tick and lot size')에 맞춘다** — 매수 올림·매도 내림(`hl_price.hl_round_price`). 안 맞으면 HL이 통째로 거부한다(실측 2026-09-07 SKHX 1,281.06 → "Price must be divisible by tick size").
- **후주문은 취소하지 않는다**(사용자 확정 2026-09-07). 지정가라 일부만 잡히고 잔량이 걸릴 수 있는데, 그래도 그대로 두고 후주문대기(HL 대기 = 잔량)에 머문다. 그동안 새 선주문은 내지 않고, 잔량이 다 잡히면 정상 흐름(ㄹ1). 엔진이 후주문에 보내는 것은 발주뿐이다. (2026-09-04에 Claude가 적었던 "선주문 딜레이 뒤 취소 → 체결차 → 중지"는 사용자 확정이 아니었고 폐기.)
- **선주문 정정 트리거(확정 2026-08-26):** 시세 수신 때마다 역산가를 다시 계산, **역산가가 바뀌면 취소 → (병행 후주문 확인) → 딜레이 → 신규주문**(§6 공통 규칙).
- **선주문 딜레이(공통설정, ms — 기본 1000):** 후주문 전량 체결 후 딜레이 뒤 다음 선주문 / 선주문 재발주 전 딜레이.
- **전환딜레이(초, 세트별):** 진입 마지막 체결 후 N초 뒤 청산 가능, 청산 마지막 체결 후 N초 뒤 진입 가능.
- **실패 처리:** 선주문 거부 → 딜레이 후 재주문 / 후주문 거부 → 체결차 → 중지 + 알림(§6 표).

### 11.5 주문 계약수
- **진입** = `Min(1회주문수량, 목표수량 − RT선진입)` / **청산** = `Min(1회주문수량, RT선진입)` / **후주문(HL)** = 선주문 체결수량 × 10.

### 11.6 선주문 가격 (역산 → 호가단위 반올림 → 진입범위)
**역산 (`disparity.maker_price_for_spread`):** 스프레드 = HL_est괴리 − 국내괴리이고, 선주문은 우리가 가격 P를 정하는 maker라 국내괴리에 P가 들어간다. 스프레드 = 기준값이 되게 P를 풀면:

```
P = 기준가_SF × (1 + HL_est괴리 − 기준값)
```

- **기준가_SF** = 선물이론가. **HL_est괴리** = 후주문 수량(=계약수×10)만큼 HL 호가창을 쓸어담은 est 괴리(환율 반영, 자동T와 동일).

| 경우 | SF | HL | HL_est 호가창 | 기준값 |
|---|---|---|---|---|
| 정방향 진입 | 매수 | 매도 | 매수호가창(bid) | `-HP/+SF` |
| 정방향 청산 | 매도 | 매수 | 매도호가창(ask) | `+HP/-SF` |
| 역방향 진입 | 매도 | 매수 | 매도호가창(ask) | `+HP/-SF` |
| 역방향 청산 | 매수 | 매도 | 매수호가창(bid) | `-HP/+SF` |

**호가단위 반올림:** 종목별 선주문 호가단위(공통설정: 하이닉스 3,000 / 삼성전자 500 / 현대차 1,000)로 SF **매수 → 내림**, SF **매도 → 올림**(기준값보다 불리해지지 않게).

**진입범위(발주 여부):**
- SF **매수**(정방향 진입·역방향 청산): 한계 = (상대**매도**호가 − 1틱) × (1 − 범위) = 가장 낮은 허용가. 발주조건 **P ≥ 한계**.
- SF **매도**(정방향 청산·역방향 진입): 한계 = (상대**매수**호가 + 1틱) × (1 + 범위) = 가장 높은 허용가. 발주조건 **P ≤ 한계**.
- **범위** = 공통설정(목업 0.4%). **상대호가** = 공통설정 콤보(`상대N호가 - 1틱`, 매도는 +1틱). **상대N호가 = 그 시점 호가창의 실제 N번째 호가**(확정 2026-08-27) — 호가가 비면 건너뛴다.
- 역산가가 한계보다 더 물러나 있으면(체결 가망 낮음) **미발주 / 이미 나가 있으면 취소**.

### 11.7 RT선진입 · 목표수량 (§9 보충)
- 진입 체결 → RT선진입 증가, 청산 체결 → 감소. **진입 가능:** RT + 계약수 ≤ 목표수량. **청산 가능:** RT ≥ 계약수.
- 목표수량 도달 후에도 계속 계산·주문 시도(청산으로 RT가 줄면 진입 여지가 다시 생김). "RT 진입수량 수동 입력"으로 덮어쓰기 가능.
- RT는 SF 계약 단위 — HL 소수 체결은 10계약이 찰 때까지 잔여분(`hl_rt_carry`)에 모았다가 올린다(2026-09-07).
- **세트별 RT선진입·체결차·순잔고(SF·HL)는 코어 상태(core_state.json)에 저장돼 코어 재시동·재접속 때 그대로 물고 온다**(사용자 확정 2026-09-07). 명령 때만이 아니라 **체결·취소·중지가 반영될 때마다 저장**한다(단일 진실=코어, 세대 백업 포함). 화면은 코어 값을 그리기만 한다.

### 11.8 체결차 (자동T와 같은 개념 — 확정 2026-08-26)
- **체결차 = SF(LS) 잔고 × 10 + HL 잔고**(매도 −), 이 세트가 낸 체결 기준. 완전 헤지면 0. 소수 그대로(예 9.412).
- ≠0 → 중지 + 알람, 사용자 수동 처리. 세트설정 "체결차 Clear"로 수동 0 초기화.

### 11.9 화면 구성 (목업 STG_2/layout_1.png)
- **상단:** 종목 콤보(현대차 제외) · HL 호가단위(숫자 틱, 일반주문창과 같은 표) · 월물 콤보(`최근`/`차근`, 기본 `최근` — DESIGN §5.11, 목업에 없던 항목) · `적` · **기준수량** · 정방향[모니터 3칸] · 역방향[3칸] · 주문가능시간 표시 · `설정`.
  - 모니터 3칸 = **[진입 SF · 진입 S · 청산 SF]**, 코어가 기준수량으로 계산한 est 괴리(%)를 정/역 각각. '적'을 누른 뒤부터 표시하고, 표시 여부는 화면 입력칸이 아니라 코어가 쓰는 기준수량(>0)으로 판단.
  - **'적'** = 종목·HL 호가단위·월물·기준수량을 한 번에 코어로(HL 호가단위가 같으면 코어가 재구독 생략). **기준수량**은 입력칸에서 **Enter**로 단독 전송(입력칸이 잠깐 파랗게 깜빡임), Enter 없이 포커스가 나가거나 Esc면 마지막 전송값으로 되돌림(수정 취소). 세트 실행 중에도 바꿀 수 있다(모니터 계산용).
  - 세트 하나라도 실행 중이면 종목·호가단위·월물 콤보와 '적'을 잠근다(상대 상품이 바뀌면 걸린 선주문과 어긋남).
- **정방향 / 역방향** 각 **진입 / 청산 3세트**, 세트별 컬럼: 목표수량 · 1회주문수량 · 진입 `-HP/+SF` · 진입 `-HP/+S` · 진입 실행 · 청산 `+HP/-SF` · 청산 실행 · `설정` · RT선진입 · 체결차 · **초**(전환딜레이) · Sprd(진입 누적) · Sprd(청산 누적). (역방향은 `+HP/-SF`·`+HP/-S`·`-HP/+SF`)
- **실행 버튼:** 캡션은 항상 '진입'/'청산'(사용자 2026-09-04), 색으로만 실행중(빨강/파랑)·중지(행 검정). 상태줄에 다리 상태(감시·걸림·부분·HL·쉼)와 선주문 번호·가격·체결·HL 대기·중지 사유. 중지 상태에서 누르면 "중지 해제" 확인창(§2 중지 행).
- **인라인 수정:** 진입 `SF`·진입 `S`·청산 `SF` 세 칸만. 진입 실행 중 → 진입 SF·S 잠금 / 청산 실행 중 → 청산 SF 잠금.
- **매매결과(진입/청산):** 그리드 위 `진입`/`청산` 버튼이 누적체결량 표시 + clear 겸용(§10).
- **세트설정 창:** 목표수량 · 1회주문수량 · 전환딜레이(초) · 진입SF · 진입S · 청산 · ☐RT 진입수량 수동 입력 · ☐체결차 Clear. **두 체크박스는 1회성**(사용자 확정 2026-09-07): 확인을 누를 때 한 번 코어로 보내고 바로 비운다 — 창을 다시 열면 꺼진 상태, 화면 저장(win_state)에도 넣지 않는다(남겨 두면 실행 켤 때마다 RT를 덮어씀). RT 수동 입력을 체크했는데 값이 비어 있으면 확인창을 띄우고 저장하지 않는다.
- **창 닫기(X):** 자동주문 화면 공통 규칙(`DESIGN-ui.md §6`) — 실행 중이면 확인창 → '예'면 전 세트 정지 후 닫기.
- **공통설정 창(체결쏴 설정):** 주문가능시간 2구간 · 선주문 호가단위(종목별) · 선주문 딜레이(ms) · 재개 딜레이(초, 시장 정지 풀린 뒤 대기 — 기본 10) · 선주문 범위(%) · 매수/매도 상대호가 콤보 · **후주문 HP(%) 매수/매도**(2026-09-04 추가) · 정/역 리스크방지. 배치(사용자 2026-09-04): 줄1 [선주문 호가단위 | 선주문(딜레이·재개·범위)], 줄2 [상대호가 | 후주문 HP].
- **리스크방지(입력 금지 + 경고):** 정방향 진입 `-HP/+SF`·`-HP/+S` ≤ `[0]` / 정방향 청산 `+HP/-SF` ≥ `[0.5]` / 정방향 `-HP/+SF` > (`+HP/-SF` + `[0.1%]`) 아니면 금지. 역방향 진입 `+HP/-SF`·`+HP/-S` ≥ `[0.5]` / 역방향 청산 `-HP/+SF` ≤ `[0]` / 역방향 (`+HP/-SF` + `[0.1%]`) < `-HP/+SF` 아니면 금지. (기본값 2026-08-27 정정)

### 11.10 주문가능시간 (2구간, 초단위)
- **사용자가 직접 입력**(확정 2026-08-26) — 공통설정창에서 2구간, 초 단위. 고정 기본값 없음. 목업값 `08:30:10~08:46:20 / 15:35:30~15:46:55`는 예시일 뿐. `DESIGN.md §6.2`의 `8:45~15:35`는 옛 표기.

### 11.11 용어
- **기준수량 · RT선진입 · 체결차 · 전환딜레이 · HP(%)** — 자동T `DESIGN-auto-t.md §8`과 동일.
- **선주문/후주문** — 선주문 = LS SF maker, 후주문 = HL taker(선주문 체결 후, 지정가).
- **선주문 딜레이 · 선주문 범위 · 선주문 호가단위 · 상대호가 콤보** — §11.4·§11.6.

---

## 결정 로그 (2026-08-27 확정)

1. 주문시간 끝나거나 실행 끄면 미체결 선주문 **취소**.
2. 선주문 일부 체결분은 **즉시** 후주문으로 헤지.
3·8. **후주문은 재호가 없음.** "후주문 부분체결"은 오타였고, 가격 변경은 **선주문**에만 있다(방식은 10번으로 정정됨). 후주문은 취소하지 않는다(11번) — 전부 체결이면 ㄹ1, HL이 거부하면 ㄹ2, 잔량이 걸려 있으면 후주문대기 유지.
4. **선주문 거부 → 딜레이 뒤 다시 냄**(멈추지 않음 — 체결 전이거나 체결분은 이미 헤지).
5. **RT선진입**은 후주문 전부 체결(헤지 완성) 후 **SF 계약수**만큼 증감.
6. 3세트·정/역 모두 **따로 돈다**(서로 간섭 없음).
7. **후주문 거부 → 체결차 → 중지+알림**(다시 안 시도). 헤지 깨진 세트는 멈춤.
9. **체결차는 방향별**로 SF잔고×10 + HL잔고.
10. (2026-09-03) **선주문 재발주는 정정이 아니라 신규주문**: 미체결 취소 → 병행 후주문 체결 확인(RT 갱신) → 딜레이 → 수량을 처음처럼 다시 계산(진입 Min(1회주문, 목표−RT) / 청산 Min(1회주문, RT))해 새 역산가에 발주. 정/역방향 진입·청산 모두 동일(§6 공통 규칙).
11. (2026-09-04) **후주문도 지정가(Gtc)만** — IOC·FOK 안 씀. 가격 = 상대 1호가 ± 후주문 HP 여유(%)(체결쏴 설정, 매수/매도 각각). **잔량은 취소하지 않는다**(사용자 확정 2026-09-07 — 09-04에 Claude가 적었던 "선주문 딜레이 뒤 취소 → 체결차 → 중지"는 폐기, §11.4). 옛 "즉시 체결형(IOC)" 전제는 폐기.

12. (2026-09-07) **후주문은 취소하지 않는다.** 잔량이 걸려 있어도 후주문대기 유지(§11.4).
13. (2026-09-07) **HL 증거금 부족은 사전 관문을 두지 않는다.** HL은 증거금보다 큰 수량도 거부하지 않고 허용분만 체결한 뒤 잔량을 버리고 'filled'로 끝낸다(실측 3회). 그 경우 선주문(SF)은 체결되고 후주문은 일부만 잡히므로 **체결차 → 중지 + 알림**으로 처리한다 — 이것이 맞는 처리(사용자 확정). 증거금 관리는 사람이 한다.
14. (2026-09-07) **판정(G5) 정정:** 진입은 **S(현물) 괴리만** 비교(정방향 `> -HP/+S`, 역방향 `< +HP/-S`), **청산은 조건 비교 없음.** SF 기준값(`-HP/+SF`·`+HP/-SF`)은 역산가에만 쓴다(§11.3). **이유(사용자):** 선물 괴리까지 AND 조건에 두면 지금 호가에서 이미 기준이 충족돼야만 주문이 나가 주문가가 너무 타이트해진다 — 선주문은 maker라 역산가(기준값이 체결로 보장되는 가격)에 미리 걸어 두면 시세가 와서 잡히는, 더 유리한 가격의 주문을 낼 수 있는데 그 기회를 막는다.

## 구현 상태 (2026-09-04)

- ① 순수 상태변화 `kp_arb/auto_m.py` — 상태·G1~G6·역산가·재발주 규칙·RT·체결차·누적 Sprd. 테스트 `tests/test_auto_m.py`.
- ② 코어 결선 `kp_arb/auto_m_engine.py` — 100ms 판정, 선주문 LS 지정가(출처 "자동M"), 후주문 HL 지정가 Gtc(상대 1호가 ± HP, HL 가격 격자 맞춤, 체결×10 — IOC 아님, 정정 2026-09-07), OrderBook 통보(발주 응답의 즉시 체결도 등록 뒤 되돌려 반영), HL 수량은 소수 그대로(체결차·HL 대기), 중지 시 에러 알람. 코어 명령 `autom_set/settings/run/release/clear_acc/stop_all`, `/state.autom_live`. 체결차는 **그 세트가 낸 체결 기준**(계좌 잔고 기준이면 수동 포지션에 걸려 켜자마자 멈춤).
- ③ 화면 `kp_arb/order_autom.py` — 정방향 실행/설정/누적/중지 행(검정) 표시, 공통 닫기 규칙, 실행 중 월물 잠금. 코어 저장값이 원본(창 열 때 1회 채움).
- 화면 진행 표시: 실행 버튼은 캡션 고정('진입'/'청산') + 색(실행중·중지), 상태줄 = 다리 상태(감시·걸림·부분·HL·쉼) + 선주문 번호·가격·체결/수량·HL 대기·중지 사유. 상단 모니터 3칸 = 기준수량 est 괴리(정/역).
- 남은 것: 역방향(§11), 운영 실측.

## [OPEN] (남은 것)

- ~~중지 시 알림 방법~~ → 확정 2026-09-04(§10: 소리 + 세트 행 검정/흰 글자 + 메인 상태줄).
- ~~시장 상황(§8)의 확인 4건~~ → 확인 완료 2026-08-27(§8 끝). 남은 것은 JIF 상태코드 매핑 표(LS 문서 기준, 구현 시).
- **역방향 진입·역방향 청산** — 정/역이 대칭이라 같은 뼈대로 추가(**정방향 먼저 완성·실측 후**, 사용자 확정 2026-09-04, §11 예정). §6 "선주문 재발주 공통 규칙"은 역방향에도 그대로 적용(확정 2026-09-03).
