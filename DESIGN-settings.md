# DESIGN-settings.md — 전체 공통설정 화면

DESIGN-manual-order §1-3/1-4가 예고한 **"설정 화면"** — 계정 단위 공통설정의 귀착지.
본격 전략 개발 전에 만든다.

## 0. 원칙
- 설정값은 **코어(`core_state.json`)에 저장** — 단일 진실. 화면은 입력·표시만.
- 일일 한도는 **코어가 집행**(화면 우회해도 막힘 — 가격제한 §1-2와 동일 사상).
- 사운드는 **메인창이 재생**(`winsound`, wav, 의존성 0). 코어가 이벤트를 스냅샷에 노출.

## 1. 일일 계좌 한도 (HL, USDC) — 사용자 확정
- **대상: HL 계좌만**, 단위 **USDC**.
- **집계: 당일 체결액(USDC) 누적만.** 취소·미체결은 안 센다. (체결액 = Σ |체결수량| × 체결가)
- **리셋: 자정(00:00 로컬)** — 날짜가 바뀌면 누적 0. *(가정 — 확인 필요)*
- **집행: HL 주문 발주 시** `당일 체결액 + 이 주문 금액(price × qty) > 한도` 면 **거부(하드블록)**.
  - 시장가(HL IOC)는 마크가로 주문 금액 추정. *(가정 — 확인 필요)*
  - 한도 = `hl_daily_limit_usdc`. **0 또는 미설정이면 한도 없음(무제한).**
- 적용 범위: 코어를 지나는 **모든 HL 주문**(수동·향후 전략·peg). 국내(LS) 주문은 대상 아님.

## 2. 사용자 알람 (사운드) — 사용자 확정
- **재생: 메인창**이 `winsound.PlaySound(path, SND_FILENAME | SND_ASYNC)`로 wav 재생.
- 이벤트 3종, 각각 **체크박스(재생 여부) + wav 파일 경로(찾아보기)**:
  - **주문 체결 시** (fill) — 국내·HL 아무 체결이나.
  - **에러 발생 시** — = **발주 거부·실패**. *(가정 — 확인 필요)*
  - **WS 끊겼을 때** — LS주식 / LS선물 / HL **아무 채널이나 연결→끊김 전이**. *(가정 — 확인 필요)*
- 설정 키: `sound_fill`{enabled, path}, `sound_error`{enabled, path}, `sound_ws`{enabled, path}.

## 3. 화면 (신규) — 메인 메뉴 "공통설정"
- 상단: **HL 일일 한도(USDC)** 입력칸.
- 하단: 알람 3줄 — `[체크박스] 이벤트명   [wav 경로]   [찾아보기]`.
- `[저장]` → 코어에 `settings_global` 전송 → `core_state.json` 저장.
- UI: `ui_theme`(기본 폰트 9 등) 일관. 메인 메뉴 "화면"에 연결.

## 4. 코어 계약
- 명령 `settings_global` {hl_daily_limit_usdc, sound_fill, sound_error, sound_ws} → 저장.
- 스냅샷(/state 또는 /manual_state)에 노출:
  - 위 설정값 + **`fill_seq`**(체결마다 +1) + **`error_seq`**(발주 거부·실패마다 +1)
  - **당일 HL 체결액(USDC)** + 한도(표시·경고용).
- 메인창: `fill_seq`/`error_seq` 증가 + WS 끊김 전이를 감지 → 해당 사운드 재생(설정 on일 때).

## 5. 구현 순서 (작은 단위, 각 단위 검증 green 후 다음)
1. **순수 로직** — 일일한도 판정 + 체결 누적(자정 리셋) + 이벤트 seq. 단위테스트.
2. **코어 배선** — HL 발주 한도 집행 + 체결 누적 + 스냅샷 노출 + `settings_global` 명령.
3. **화면** — 공통설정 창 + 메인 메뉴 연결.
4. **메인창** — 사운드 재생(winsound) + 이벤트 감지.

## 6. [OPEN] 확인 필요
- 리셋 시각 = 자정으로 OK?
- '에러' = 발주 거부·실패로 한정 OK? (다른 에러도 포함?)
- 'WS 끊김' = 아무 채널이나 끊기면 OK?
- 시장가 주문 금액 추정 = 마크가 OK?
