# DESIGN-fx-auction.md — 원달러선물 동시호가 대응주문 (§9.1 KR_FX 발주 구현)

주식선물(삼성·하이닉스) **신규주문을 감지**해, 동시호가 시간대에 **원달러선물로 환헤지 대응주문**을 자동 발주하는 전략 + 화면. DESIGN §9.1의 `KR_FX` 발주 흐름([OPEN])을 구현한다. 이 문서가 계약이다.

## 1. 대상·계좌
- **감시 대상**: 삼성전자·SK하이닉스 **주식선물**(`KR_STOCK_FUTURE`, 계좌 `KR_DERIV`).
- **대응 발주**: **원달러선물**(신규 instrument `KR_FX_FUTURE`, 계좌 `KR_FX`).
- 원달러선물 승수/계약: **1계약 = US$10,000**, 호가단위(틱) **0.1원**.
- 주식선물 승수: **1계약 = 10주**.

## 2. 시간창 (화면 입력, 4칸)
- **장전** 시작~종료, **장마감후** 시작~종료 (예 08:30~08:46 / 15:35~15:46).
- 이 **두 구간 안에서 감지된 신규주문만** 대응. 밖이면 무시. (동시호가 중이라 체결통보는 안 옴 — 신규만 처리.)

## 3. 트리거 — 외부(HTS) 신규주문 감지
- 주식선물 주문은 **HTS 등 외부**에서 내고, 우리는 **LS 주문 WS 접수 이벤트(선물 O01 = `OrderEvent.kind="ack"`)로 감지만** 한다(계좌 등록 tr_type="1" → 외부 주문도 수신).
- **신규만**: 구분/타입에 정정·취소가 없으므로 **원주문번호(`org_order_id`) 유무로 판정** — 있으면 정정/취소로 보고 **제외**, 없으면 신규.
- 접수 이벤트의 **원본(`OrderEvent.body`)** 에서 매매구분·계약수·주문가·종목코드를 뽑는다(대상 2종목만).
- 같은 주문번호(order_id)는 **1회만** 대응(중복 접수 방지).

## 4. 대응주문 계산 (순수 로직 — `fx_auction.py`)
- **방향**(주식선물 → 원달러선물, 반대):
  - 매수 → **매도**, 가격 = `현재가 − 주문틱`
  - 매도 → **매수**, 가격 = `현재가 + 주문틱`
- **주문틱 = 틱개수 × 0.1** (원달러선물 1틱=0.1원). 예: 10틱 → ±1.0. 결과는 0.1 그리드로 반올림.
- **수량 = 반내림( 계약수 × 10 × 주식선물주문가 ÷ 현재가 ÷ 10000 × 헤지비율 )** — 헤지비율(예 50%=0.5)을 **반내림 안**에 넣어 **정수 계약**. 0이면 발주 안 함.
- **현재가**: 화면 입력 **고정값**(실시간 아님). 수량·가격 모두 이 값 사용.

## 5. 화면 — 원달러선물 동시호가 주문 (`fx_auction_order.py`)
목업: `docs/동시호가주문_run.png`(실행중) · `docs/동시호가_stop.png`(정지).
- **주문시간**: 4개 입력칸(장전 시작~종료 / 장마감후 시작~종료).
- **종목**: 콤보(최근월물·차근월물) — 각 **종목코드는 LS 조회값**(`_fx_months` 근·차근)으로 채움.
- **현재가**·**틱**(개수): 원달러선물 주문가 계산 인자(수동 입력).
- **헤지비율**(%): 수량 계산 인자.
- **대응주문 개시** 체크: 체크돼야 **실행** 버튼 클릭 가능(게이트).
- **실행/자동주문실행중** 토글 버튼:
  - 실행 클릭 → 캡션 **"자동주문실행중"**, **노란색**, 감시 시작.
  - 자동주문실행중 클릭 → **중지**: 캡션 "실행", 색 원복, 감시 정지, **대응주문 개시 체크 해제**.
- 하단 안내: `* 삼전닉스 주식선물 신규 주문일때만 대응주문` + 시계.
- 화면 스레드 네트워크 금지(폴링/전송은 뒷단, 화면은 결과만). 스타일은 ui_theme.

## 6. 코어 연동
- **명령**(화면 → 코어): `fx_auction_start`{windows, fx_code, price, tick, hedge_ratio}, `fx_auction_stop`{}.
- **감시 로직은 코어**: 실행 중이면 주식선물 접수 이벤트를 위 규칙으로 걸러 `KR_FX`로 발주.
- **스냅샷 노출**: 근·차근 월물 코드(콤보용), 실행 상태.

## 7. 구현 현황 (있음 / 만들 것)
- ✅ `Account.KR_FX`, 원달러선물 근·차근 코드(`_fx_months`)·현재가, `OrderEvent.body`(접수 원본), 외부주문 수신 WS.
- ⬜ `Instrument.KR_FX_FUTURE` + `routing.account_for` → `KR_FX`.
- ⬜ **LS 원달러선물 발주 TR**(주식선물 발주 TR 참고 — 확인 후 구현).
- ⬜ 접수 이벤트에서 매매·계약수·주문가·종목 추출(선물 O01 body 필드 확인).
- ⬜ 순수 로직 `fx_auction.py`(시간창·수량·가격·방향) + 단위테스트.
- ⬜ 코어 감시 태스크 + 명령(start/stop) + 스냅샷 노출.
- ⬜ 화면 `fx_auction_order.py` + main 메뉴 연결(main.bat 단일 진입).

## 8. 불변식·주의
- 원달러선물 발주는 **KR_FX 계좌 전용**(다른 노출은 여전히 fx.usd_exposure로 외부#2 보고만).
- **정정·취소는 대응 안 함**(신규만). 시간창 밖 무시.
- 라이브 발주 경로라 **테스트는 목/리플레이만**(라이브 LS 호출 금지).
- `[OPEN]` — 원달러선물 발주 TR 코드/파라미터, O01 body 정확한 키(실측 확인 필요).
