# DESIGN-ui.md — 화면 공통 스타일 계약 (tkinter 클라이언트)

이 문서는 모든 창(order_hl, order_list, monitor, fx_monitor, main_window …)이 따르는 **UI 일관성 규칙**이다. **색·역할**은 개발 완료된 일반주문창(order_hl) 기준이고, **기본 폰트는 일반 창(base 9)** 기준이다(대부분 창이 9, order_hl처럼 정보밀도 높은 큰 화면만 예외로 11). 값은 `kp_arb/ui_theme.py`에 토큰으로 굳혔다.

## 0. 원칙
- 화면은 **raw 폰트 튜플·색 문자열을 직접 쓰지 않는다.** `ui_theme`의 **역할 토큰만** 참조한다.
- 크기·색을 바꾸려면 **`ui_theme.py` 한 곳만** 고친다(전 화면 일괄 반영).
- 새 창은 **처음부터** `ui_theme`를 쓴다. 기존 창은 점진 이전.

## 1. 폰트 (family = `Malgun Gothic`)

| 토큰 | 값 | 용도 |
|---|---|---|
| `FONT_BASE` | 9 | 창 기본(라벨·버튼·체크박스) — `apply_base`가 `*Font`로 지정 |
| `FONT_NUM` | 9 **bold** | **숫자·입력값** — *숫자는 볼드* 규칙 |
| `FONT_LABEL` | 9 | 표 제목·상태바·콤보 |
| `FONT_SMALL` | 8 | 보조 캡션(Cross·Unified 등) |
| `FONT_GRID` | 10 **bold** | 표(Treeview: 호가창 등), 행높이 `ROWHEIGHT=22` |
| `FONT_STRONG` | 14 **bold** | 강조 버튼(주문 버튼) |
| `FONT_BASE_LG` | 11 | **큰 화면 기본** — 정보밀도 높은 일반주문창(order_hl) 등 예외 |
| `FONT_NUM_LG` | 11 **bold** | 큰 화면 숫자·입력값 |

## 2. 색 (의미별)

| 토큰 | 값 | 의미 |
|---|---|---|
| `C_BUY` | `#c00000` | 매수·이익(빨강) |
| `C_SELL` | `#0000c0` | 매도·손실(파랑) |
| `C_ZERO` | `black` | 0·중립 |
| `C_BUY_ACTIVE` / `C_SELL_ACTIVE` | `#a00000` / `#000090` | 매수/매도 버튼 눌림 |
| `C_HILITE_BG` | `#ffe680` | 강조 바탕(잔고·PNL) |
| `C_QUERY_LBL` | `gray45` | 조회(REST) 항목 라벨 — 회색 구분 |
| `C_ERR` | `#8b0000` | 거부·실패(상태바) |
| `C_MUTED` | `gray30` | 보조·안내(상태바 일반) |
| `C_BORDER` | `gray50` | 셀 테두리 |

`sign_color(v)`: 양수→`C_BUY` / 음수→`C_SELL` / 0·비수치→`C_ZERO`. (잔고·PNL 등 부호 색)

## 3. 규칙 요약
- **숫자·입력값은 `FONT_NUM`(볼드).** 라벨은 `FONT_BASE`/`FONT_LABEL`.
- **매수=빨강, 매도=파랑**(주문 버튼·부호 색 일관). 이익/손실도 같은 색 규약(`sign_color`).
- **조회(REST) 값 라벨은 회색**(`C_QUERY_LBL`)으로 즉시계산 값과 구분.
- 창 생성 직후 **`T.apply_base(root)`** 한 줄로 기본 폰트·표 스타일 적용.

## 4. 셀 단위 색이 필요한 표
- **`ttk.Treeview`는 행 단위 색(tags)만** 되고 셀 단위는 안 된다. 셀마다 색이 필요하면
  표를 **`tk.Label` 그리드**(스크롤 캔버스)로 짠다 — 각 Label이 자기 fg/bg를 가짐.
- 대신 Treeview가 공짜로 주던 **행 선택·스크롤·가상화**를 직접 구현해야 하고, 행이 많으면
  **위젯 재사용(행 풀)** 으로 갱신 비용을 낮춘다. (예: `order_list` — 매매 칸만 색)
- 작은 고정 표(잔고칸 ~10행)는 Label 그리드가 자연스럽고, 매우 긴 목록은 Treeview가 유리.

## 6. 자동주문 화면 공통 — 창 닫기(X) 규칙 (사용자 확정 2026-09-04)
자동주문(원달러선물 동시호가·자동T·자동M, 앞으로 생기는 것 전부)은 **코어가 돌리고 창은 보기·조작만**
한다(DESIGN §12 "코어 하나 + 여러 화면"). 그래서 창을 닫아도 실행은 남는다(운영 실측 2026-09-04 —
동시호가 창을 닫았는데 대응주문이 계속 나감). 모든 자동주문 화면은 같은 규칙으로 닫는다:
- **실행 중 아님** → 그냥 닫는다.
- **실행 중** → 확인창 "자동주문이 실행 중입니다. 자동주문을 정지하고 화면을 닫을까요?"
  - **예** → 코어에 정지 명령 → 코어 상태에서 실행이 풀린 것을 확인한 뒤(최대 3초) 닫는다.
    (창을 바로 닫으면 전송 스레드가 같이 죽어 정지 명령이 유실될 수 있다.)
  - **아니오** → 창 유지. 실수 클릭 방지용 확인이며, "닫으면서 실행을 남기는" 선택지는 없다.
- 구현: `kp_arb/ui_close.py` — `attach_auto_close(root, title, is_running, send_stop, set_status)` 한 줄.
  판정은 순수 함수 `close_plan(running, confirmed)`. 적용: ✅ 동시호가 창 · ✅ 자동M(2026-09-04,
  실행 중 = 정방향 세트 중 하나라도 running, '예'면 `autom_stop_all`) · ⬜ 자동T(실행 상태변화와 함께).

## 7. 팝업(확인·알림)은 항상 그 화면(부모 창) 중앙 (사용자 확정 2026-09-04)
- tk `messagebox`는 위치를 정할 수 없어 창이 구석에 있으면 엉뚱한 곳에 뜬다 → 쓰지 않는다.
  `kp_arb/ui_dialog.py`의 `ask_yes_no(parent, title, msg)` / `show_message(parent, title, msg)`
  (모달, **부모 창 중앙**, 최상위, Enter=첫 버튼·Esc=아니오)를 쓴다. 부모가 아직 안 보이면 모니터 중앙.
- 화면이 직접 만드는 Toplevel 팝업(세트 설정·리스크방지 경고 등)도 `center_on_parent(win, root)`로 같은 위치.
- **멀티모니터(실측 2026-09-09):** tk는 주 모니터 크기만 알고 옛 계산은 음수 좌표를 0으로 밀어, 부모 창이 다른 모니터에 있으면 팝업이 보이는 범위 밖에 떠 모달 잠금으로 창 전체가 안 눌렸다. 이제 부모 창이 있는 모니터의 작업 영역(Win32 MonitorFromWindow)을 구해 팝업이 그 안에 다 들어오게 맞춘다(음수 좌표 허용). 영역을 못 얻으면 옛 방식.
- **메인창 화면 메뉴 게이트:** 코어가 연결되기 전엔 화면을 열지 않는다 — 메뉴를 누르면 "코어 연결 전"
  알림만(사용자 2026-09-04: 코어 시동 중 성급히 연 창이 미접속 상태로 뜨는 것 방지). 저장된 화면
  자동 복원은 원래 연결 뒤에만 돈다 — 코어 시동은 LS 순차 조회 때문에 30~60초 걸리므로 **120초**까지
  기다린다(옛 30초는 시동 중 만료돼 복원이 안 됐음, 2026-09-07). 메인의 코어 감시(미접속 자동 재기동)도
  같은 이유로 부팅 유예 약 120초 — 짧으면 뜨는 중인 코어 옆에 두 번째 코어를 띄워 LS 조회·토큰이 겹치고
  포트 충돌로 죽는다.
- **저장 목록 보존:** ui_state.json은 2초마다 저장되는데, 저장된 화면을 아직 다시 열기 전(시동 대기·복원
  포기)에는 "지금 열린 창"(빈 목록)으로 덮어쓰지 않고 이전 목록을 그대로 둔다(`screens_to_save`). 복원이
  돌았거나 사용자가 메뉴로 창을 하나라도 연 뒤부터 실제 열린 창이 진실이다(2026-09-07).
- **상태 파일 저장 공통(`kp_arb/state_backup.py`, 사용자 확정 2026-09-07):** ui_state.json·core_state.json·
  .win_state/*.json은 (1) 내용이 바뀔 때만 쓰고 (2) 임시 파일 → 이름 바꾸기로 원자적으로 교체하며
  (3) 새로 쓸 때 기존 파일을 `backup/<이름>.1.json`으로 밀어 세대 백업을 둔다(1→2→…, ui_state·core_state
  5세대, 창 상태 3세대 — 개수 고정이라 날짜별 백업처럼 쌓이지 않음). 빈 화면 목록 저장은 위 보존 규칙이 막는다.
- **메인 메뉴 "화면 구성 되돌리기":** ui_state 세대를 저장 시각 + 화면 이름으로 보여 주고 고르면 그 세대를
  현재로 되돌리고(지금 구성은 1세대로 밀림 — 되돌리기도 되돌릴 수 있음) 아직 안 열린 화면을 연다.
  코어 연결 전에는 안내만.

## 8. 메인 창 최소화·복원을 모든 화면이 따라간다 (사용자 확정 2026-09-17)
- 다른 프로그램(HTS 등)처럼 **메인을 최소화하면 열린 화면 전부 최소화, 메인을 복원하면 전부 복원**.
- 화면은 메인의 자식 창이 아니라 **별도 프로세스**라 OS가 묶어 주지 않는다. 메인이 자기 창 핸들을 환경변수 `KP_MAIN_HWND`로 넘기고, 각 화면이 `win_state.attach` 안에서 0.3초마다 메인 창의 최소화 여부를 보고 따라간다(`win_state.follow_main_minimize`).
- 사용자가 화면 하나만 따로 최소화해 둔 경우, 메인 복원 때 그 창은 건드리지 않는다(메인 때문에 최소화된 창만 복원). 작업표시줄 단추는 창마다 그대로.
- Windows 전용. 환경변수가 없으면(단독 실행·미리보기) 아무 것도 하지 않는다.
- **작업표시줄 단추는 하나**(사용자 2026-09-17): 모든 창이 같은 앱 식별자(`kp-arb.meme`, `win_state.set_taskbar_group`)를 갖는다. 개발 실행은 메인(pythonw)·화면(python) 실행 파일이 달라 둘로 갈렸었다.
- 알려진 한계(사용자 2026-09-17 "이 상태로 두자"): 묶음 단추에서 화면 하나를 골라 올리면 그 창만 올라오고 메인은 최소화로 남는다(메인을 골라 올리면 전부 따라옴). "화면 복원 → 메인 복원" 연동과 소유 창 방식은 보류.

## 5. 적용 상태 (추적)

- ✅ 토큰 정의: `kp_arb/ui_theme.py` (+ `tests/test_ui_theme.py`)
- ✅ `order_autom_stock` — 체결쏴 주식 화면(order_autom 사양 분기, 2026-09-16 골격 확정: exec 결정 40; 코어 연결 전 `--preview` 전용)
- ✅ `order_list` — ui_theme 적용 + **Treeview 표**(2026-09-16 Label 그리드에서 전환, 사용자 확정: 코어가 당일 체결·취소 전부를 보내 수백 행이 되자 그리드는 체결마다 0.2초 멈칫. 색은 행 단위 매수=빨강/매도=파랑, HL 체결 창과 같음. 캡처 docs/preview/order_list_tree.png)
- ✅ `hl_trades`(2026-09-15) — ui_theme 토큰으로 처음부터(Treeview 행 태그 매수=빨강/매도=파랑)
- ⬜ order_hl 이전(값 동일 → 무변화) · monitor · fx_monitor · main_window
- ⬜ [OPEN] 숫자 표시 포맷(`_fmt`/`_fmt_qty`/`_fmt_px`) 공용화 — 현재 창별 중복
