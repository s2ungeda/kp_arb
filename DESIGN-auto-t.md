# DESIGN-auto-t.md — 자동T 전략 (바로쏴, HL–주식 동시 taker)

> **상태:** 작성 중(초안). `DESIGN.md §6.2`의 자동T 부분을 떼어 상세화한다. **계약의 일부.**
> **원본:** 화면 목업 `docs/STG_1/`(`layout_1.png`·`layout_run.png`·`전략별 설정.png`·`화면내 공통설정.png`, 갱신 2026-08-24) + 사용자 구술·문답(2026-08-24, `바로쏴(자동T).txt`).
> **화면 초안 코드:** `kp_arb/order_panel.py`(자동T/자동M 겸용, 코어 클라이언트). **아직 발주 로직 미구현.**

## 1. 개요
- 자동T = **HL perp ↔ 국내 주식(S)** 한 쌍, **양쪽 동시 taker**(반드시 체결). maker 방식 자동M(HL–주식선물)과 별개 화면.
- 주로 **프리장·애프터장**에서 사용(장중은 자동M 위주).
- 한 종목에 **정방향·역방향을 동시 모니터하되 배타적 진입**(고/저평가에 따라 한쪽만).

## 2. 방향 정의 (사용자 확정 2026-08-24)
| 방향 | 시장 상태 | 진입 액션 | 청산 액션 |
|---|---|---|---|
| **정방향** | HL 고평가 / LS 저평가 | `-HP/+S` : HL 매도 + LS 매수(**융자**) | `+HP/-S` : HL 매수 + LS 매도 |
| **역방향** | LS 고평가 / HL 저평가 | `+HP/-S` : HL 매수 + LS 매도(**대주**) | `-HP/+S` : HL 매도 + LS 매수 |

- **대주(대차 공매도) 허용**(시장 기회 시, 설정창 주문유형 옵션) — `DESIGN §3` 공매도 불변식의 자동T 예외.

## 3. 판정 (신호 = 발주 수량 est-price 스프레드, `DESIGN §6.1-2`)
각 세트는 **1회주문수량으로 est-price**를 구해 실시간 진입/청산 스프레드를 계산한다.

| 신호(컬럼) | 스프레드 | 발주 조건 | 액션 |
|---|---|---|---|
| 정방향 진입 (`-HP/+S`) | en | 실시간 **> 진입 기준값** | HL 매도 + LS 매수 |
| 정방향 청산 (`+HP/-S`) | ex | 실시간 **< 청산 기준값** | HL 매수 + LS 매도 |
| 역방향 진입 (`+HP/-S`) | ex | 실시간 **< 진입 기준값** | HL 매수 + LS 매도 |
| 역방향 청산 (`-HP/+S`) | en | 실시간 **> 청산 기준값** | HL 매도 + LS 매수 |

- 정방향: 스프레드가 **클 때(HL 고평가) 진입**, 작아지면 청산. 역방향: 반대로 **LS 고평가일 때 진입**(기준값은 음수 예: −1.5/−1.2/−0.9), 줄어들면 청산.
- **컬럼↔스프레드 대응(확정 2026-08-24):** `-HP/+S`=en(진입 스프레드), `+HP/-S`=ex(청산 스프레드).
- **신호값 = `DESIGN §6.1-2` est 스프레드 그대로**(양쪽 est, 진입 bid/bid·청산 ask/ask). 자동T도 신호는 이 값으로 판정하고, **실제 발주가는 별도 taker 공식(§4a)**으로 낸다(확정 2026-08-24 — 신호는 est, 발주가는 taker로 분리).

## 4. 실행 (동시 taker)
- 발주 조건 충족 → **HL·LS에 동시 주문**(둘 다 taker: §4a 여유가로 반드시 체결).
- **양쪽 모두 체결 확인 후** 다음 주문 발주. 세트 간 **전환딜레이(초)** 대기.
- 어느 한쪽이 **미체결·일부체결** → **시스템 정지 + 사용자 알람**(재주문·추가주문 안 함).

## 4a. 주문 가격 (taker, 반드시 체결 — 확정 2026-08-24)
발주가는 **현재 1호가 기준으로 반대로 넉넉히 밀어** taker 체결을 보장한다(설정: 매수/매도 각 LS `tick`·HP `%`).

- **매수:** LS = **매수1호가 + 설정틱**, HL = **매수1호가 × (1 + HP%)** (위로)
- **매도:** LS = **매도1호가 − 설정틱**, HL = **매도1호가 × (1 − HP%)** (아래로) — 확정 2026-08-24
- 목업 기본: LS 10틱 / HP 15%. (신호 est 스프레드와 별개 — est는 판정용, 이 값은 실제 발주가.)

## 5. RT선진입 · 목표수량
- 진입 체결 → **RT선진입 증가**, 청산 체결 → **감소**(LS 기준, 역방향도 +로 표기).
- **진입 가능 조건:** RT선진입 + 1회주문수량 ≤ **목표수량**(초과 불가).
- **청산 가능 조건:** RT선진입 ≥ **1회주문수량**.
- **목표수량 도달 후에도 계속 계산·주문 시도**한다 — 청산으로 RT선진입이 줄어 다시 진입 여지가 생기므로.

## 6. 체결차
- **체결차 = 주식(LS) 잔고 + HL 잔고** (매도는 − 부호). 완전 헤지면 0.
- **≠0(양쪽 체결 불일치) → 시스템 정지 + 알람.** 재/추가 주문은 하지 않고 **사용자가 수동 처리**. 세트 설정의 "체결차 Clear"로 수동 0 초기화.

## 7. 화면 구성 (목업 STG_1, 갱신 2026-08-24)
- **상단:** 종목 콤보 · HL 호가단위 · `적` · **기준수량** · 정방향[진입 est / 청산 est] · 역방향[진입 est / 청산 est]
- **2번째 줄:** 주문가능시간 2구간 · `S 제외여부` · `설정`(공통설정 창) — **진입한도 삭제됨(2026-08-24)**
- **정방향 / 역방향** 각 **진입 / 청산 3세트**, 세트별 컬럼: 목표수량 · 1회주문수량 · 기준값 · 실행 버튼(진입/청산) · `설정` · RT선진입 · 체결차 · **초**(전환딜레이) · Sprd(−HP/+S/−환 등)
- **세트 설정 창:** 목표수량 · 1회주문수량 · **전환딜레이(초)** · 진입 기준값 · 청산 기준값 · ☐RT 진입수량 수동 입력 · ☐체결차 Clear
- **공통설정 창(바로쏴 설정):** 주문가능시간 2구간 · S 제외여부 · LS(tick)/HP(%) 매수·매도 · 주문유형(정/역 진입·청산)

## 8. 용어 사전 (사용자 구술 2026-08-24)
| 용어 | 뜻 |
|---|---|
| **기준수량** | 상단 정/역방향 **모니터 수치(진입/청산 est 스프레드) 계산용 주문수량**. §6.1-2 est-price의 그 수량 |
| **RT선진입** | 목표진입량 중 **실제 체결로 진입된 수량**(LS 기준, 역방향도 +). "RT 진입수량 수동 입력"으로 덮어쓰기 가능 |
| **체결차** | LS 잔고 + HL 잔고(매도 −). 0=헤지 정합, ≠0=불일치 → 정지 |
| **전환딜레이(초)** | 한 주문 체결 후 다음 주문까지 대기 시간(세트별) |
| **HP(%)** | taker 주문가를 현재가 기준 ±몇 %로(반드시 체결용, 예 15%). LS는 ±tick |
| **S 제외** | 국내(S) 주문 빼고 **HL만 발주** |

## 9. 파라미터 · 설정
- **주문유형(LS 신용거래 코드)** — 일반 `00 보통`. 화면 공통설정에서 선택:
  `003 유통/자기융자신규` · `005 유통대주신규` · `007 자기대주신규` ·
  `101 유통융자상환` · `103 자기융자상환` · `105 유통대주상환` · `107 자기대주상환` · `180 예탁담보대출상환(신용)`
  - 정방향 진입=유통/자기융자신규 / 청산=유통융자상환, 역방향 진입=자기대주신규 / 청산=유통대주상환.
- **HP(%)·LS(tick) 여유:** 매수·매도 각각(목업 HP 15% / LS 10틱). taker 주문가 = 현재가 ± 이 폭.
- **전환딜레이(초):** 세트별, 다음 주문까지 대기(목업 30).
- **기준수량:** 상단 모니터 est 스프레드용.

## 10. 주문가능시간 (2구간, 목업값)
- **1구간 = 프리장, 2구간 = 애프터장** (사용자 확정 2026-08-24). 자동 발주는 이 두 구간에서만.
- **초 단위까지** 사용자가 화면에서 입력(예: `08:30:10 ~ 08:46:20`). 목업 숫자는 예시 — 고정 기본값 아님.

## [OPEN]
- 정/역방향 **동시 진입 방지**(배타) 구현 상세 — 사용자 고민 중(2026-08-24).
