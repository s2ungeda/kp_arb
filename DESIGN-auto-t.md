# DESIGN-auto-t.md — 자동T 전략 (바로쏴, HL–주식 동시 taker)

> **상태:** 작성 중(초안). `DESIGN.md §6.2`의 자동T 부분을 떼어 상세화한다. **계약의 일부.**
> **원본:** 화면 목업 `docs/STG_1/`(`layout_1.png`·`layout_run.png`·`전략별 설정.png`·`화면내 공통설정.png`) + 사용자 구술(2026-08-24). 빈 `바로쏴(자동T).txt`는 자리표시.
> **화면 초안 코드:** `kp_arb/order_panel.py`(자동T/자동M 겸용, 코어 클라이언트).

## 1. 개요
- 자동T = **HL perp ↔ 국내 주식(S)** 한 쌍, **양쪽 동시 taker**(반드시 체결). maker 방식 자동M(HL–주식선물)과 별개 화면.
- 주로 **프리장·애프터장**에서 사용(장중은 자동M 위주) — 운영시간 2구간(§5).
- 한 종목에 **정방향·역방향을 동시 모니터하되 배타적 진입**(고/저평가에 따라 한쪽만).

## 2. 방향 정의 (사용자 확정 2026-08-24)
| 방향 | 조건 | 국내(LS) | HL |
|---|---|---|---|
| **정방향** | LS 저평가 + HL 고평가 | 매수(**융자**) | 숏(−HP) |
| **역방향** | LS 고평가 + HL 저평가 | 매도(**대주** = 대차 공매도) | 롱(+HP) |

- **대주(공매도) 허용:** 원래 국내 spot 공매도 미사용이 계약이었으나, **시장 기회가 나오면 대차를 통해 공매도할 수 있어야 한다**(사용자 확정). 그래서 설정창에 주문유형 옵션을 둔다. → `DESIGN §3` 공매도 불변식의 **자동T 예외**로 명문화.

## 3. 화면 구성 (목업 STG_1 대조)
- **상단:** 종목 콤보 · HL 호가단위 · `적` · **기준수량** · 정방향[진입 est / 청산 est] · 역방향[진입 est / 청산 est]
- **2번째 줄:** 주문가능시간 2구간 · 진입한도 · `S 제외여부` · `설정`(화면내 공통설정 창)
- **정방향 / 역방향** 각 **진입 / 청산 3세트**, 세트별 컬럼: 목표수량 · 1회주문수량 · 기준값 · **실행 버튼(진입/청산)**(실행 중 노랑) · `설정`(세트 설정 창) · RT선진입 · 체결차 · Sprd
- Sprd 구성 표시(우측): 정방향 −HP / +S / −환, 역방향 +HP / −S / +환 — [OPEN] 정확한 서식·갱신.

## 4. 용어 사전 (사용자 구술 2026-08-24)
| 용어 | 뜻 |
|---|---|
| **기준수량** | 상단 정/역방향 **모니터 수치(진입/청산 est 스프레드) 계산용 주문수량**. §6.1-2 est-price의 그 수량과 동일 |
| **RT선진입** | 목표진입량 중 **실제 체결로 진입된 수량**(LS 기준, 역방향이어도 +로 표기). 세트설정 "RT 진입수량 수동 입력"으로 사람이 덮어쓸 수 있음 |
| **체결차** | HL과 LS의 **체결 수량 차이**. "체결차 Clear"로 수동 0 초기화 |
| **진입한도** | 한 화면에서 진입 가능한 **최대 잔고수량(LS 기준)** — 금액 아님, 수량 |
| **HP(%)** | taker 주문가를 **현재가 기준 ±몇 %**로 잡을지(사용자 입력). 이번 전략은 전부 taker라 반드시 체결돼야 하므로 넉넉히(예 15%). LS는 ±tick |
| **S 제외** | 국내(S) 주문을 빼고 **HL만** 발주하는 모드 — [OPEN] 확인 |

## 5. 파라미터 · 설정
- **주문유형(LS 신용거래 코드)** — 일반 `00 보통`. 화면 공통설정에서 선택:
  `003 유통/자기융자신규` · `005 유통대주신규` · `007 자기대주신규` ·
  `101 유통융자상환` · `103 자기융자상환` · `105 유통대주상환` · `107 자기대주상환` · `180 예탁담보대출상환(신용)`
  - 정방향 진입=융자신규 / 청산=융자상환, 역방향 진입=대주신규 / 청산=대주상환.
- **HP(%)·LS(tick) 여유:** 매수·매도 각각. taker 주문가 = 현재가 ± 이 폭(반드시 체결). 기본값 목업 = LS 10틱 / HP 15%.
- **진입한도:** 화면당 최대 진입 잔고수량(LS 기준).
- **주문가능시간 2구간:** 자동T가 자동 발주하는 두 시간대(프리장·애프터장 추정) — [OPEN] 각 구간 확정.

## 6. 판정 · 실행  [OPEN — 사용자 설명 예정]
- 진입/청산 스프레드는 `DESIGN §6.1-2` **est-price 기반**(기준수량). 정/역방향 기준값과의 **비교 부등호·부호** = [OPEN].
- 동시 taker **발주 순서**, 한쪽 **거부·부분체결** 처리(§6.2-5: 거부 시 중단+알림) = [OPEN 상세].
- **RT선진입 누적**·목표수량 도달 시 동작(완료 표시 등) = [OPEN].
- **체결차** 발생 시 대응(수동 Clear 외 자동 보정 여부) = [OPEN].
